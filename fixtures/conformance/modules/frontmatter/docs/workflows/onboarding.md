---
type: workflow
title: Onboarding a new contributor to the repository, the toolchain, the review conventions and the release
description: Everything a first-time contributor has to be shown, in order.
---

<!-- expect: FAILS -->

`title` is 101 characters against `maxLength: 80`, so `VALUE_TOO_LONG` fires. The
count is of characters, because `maxLength` applies to strings only.
