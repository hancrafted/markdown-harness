# Legacy_Export_2019

<!-- expect: PASSES -->

Uppercase and underscores, and not a single finding. This rule asks ONE question — is the stem at most 40
characters — and answers only that. A file no constraint names is unreported even under a rule that selected
it, which is tenet 6 holding one level down from paths.
