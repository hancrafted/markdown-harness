# wiki — llm wiki

<!-- expect: FAILS -->

`wiki` is not in the category's closed set, so `FILE_NAMES__VALUE_NOT_ALLOWED` fires at `file.category`.
The violation carries the whole `allowed` list with each value's `intent`, so a Contributor learns what was
permitted without opening the config.
