# aikb

<!-- expect: FAILS -->

One part where two are declared, so `FILE_NAMES__TOO_FEW_SEGMENTS` fires at `file` — the subject's own
address, because no single declared part is at fault. This is the case that killed optional trailing
segments: a stem with no delimiter is the COMMON case, 40 of 73 stems here, so an optional category would
let every undelimited name pass a category check by having no category.
