# aikb — llm wiki

<!-- expect: FAILS -->

A LEADING hyphen, the second of the three hyphen edges `kebab-case` refuses. POSIX also forbids a leading
`-` in a portable filename, so this shape was never safe to admit.
