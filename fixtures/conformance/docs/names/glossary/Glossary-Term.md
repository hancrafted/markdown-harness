# Glossary Term

<!-- expect: FAILS -->

`FILE_NAMES__FORMAT_MISMATCH` at `file.term`. The simplest possible naming violation, and the one an
adopter will hit first.

The stem avoids a case-only collision with `corpus.md` for the reason recorded in
`../blocks/AIKB__wiki-index.md`.
