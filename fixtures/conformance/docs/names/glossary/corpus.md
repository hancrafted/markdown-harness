# corpus

<!-- expect: PASSES -->

A ONE-segment name, which is a stem with no delimiter at all. The degenerate count is a real shape, not an
edge to be routed around: it is how a Module governs the form of an undelimited name.
