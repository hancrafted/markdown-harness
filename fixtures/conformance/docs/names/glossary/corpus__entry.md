# corpus — entry

<!-- expect: FAILS -->

THE `__`-WHERE-ONE-SEGMENT-IS-DECLARED CASE. The rule declares a single segment, so the delimiter is
meaningful here and the stem splits into two parts: `FILE_NAMES__TOO_MANY_SEGMENTS`.

Compare `archive/legacy__export.md`, where the identical spelling is unremarkable. `__` means something only
where `segments:` is declared, and this pair is the evidence.
