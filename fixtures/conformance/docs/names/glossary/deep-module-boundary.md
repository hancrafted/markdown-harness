# deep module boundary

<!-- expect: PASSES -->

Exactly 20 characters against `maxLength: 20`. The bound is INCLUSIVE, and the boundary is stated as a case
because "at the cap" is the value a reimplementation is most likely to get wrong by one.
