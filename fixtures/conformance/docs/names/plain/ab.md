# ab

<!-- expect: FAILS -->

Two characters against `minLength: 3`, so `FILE_NAMES__VALUE_TOO_SHORT` fires at `file` — the subject's own
address, with no `segments` roster in the requirement. The roster's ABSENCE is what marks a finding as a
whole-stem one.
