# release notes

<!-- expect: PASSES -->

The whole-stem shape: no `segments:`, so the stem is judged as one string against `minLength`, `maxLength`
and `format` together. `release-notes` satisfies all three.
