# mh63

<!-- expect: FAILS -->

No hyphen, so `FILE_NAMES__PATTERN_MISMATCH` fires at `file`. The report carries the Operator's sentence —
"a project prefix, a hyphen, then the ticket number" — and never the raw regex.
