# mh 63

<!-- expect: PASSES -->

The expressiveness valve: a `pattern` on the whole stem, for a shape no named format can express. Its
sibling `intent` is mandatory and is what a violation would report.
