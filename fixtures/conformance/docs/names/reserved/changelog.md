# changelog

<!-- expect: FAILS -->

`changelog` is outside the closed set, so `FILE_NAMES__VALUE_NOT_ALLOWED` fires at `file`. The violation
prints the permitted set back with each value's `intent`, uncapped.
