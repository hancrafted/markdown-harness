# readme

<!-- expect: PASSES -->

`allowed` over the WHOLE STEM, strings only. The value is `readme` rather than `README.md`: the subject
measures the stem, with the final `.md` removed, because `isMarkdownFile` already makes the extension the
only possibility and a config asked to describe it would be describing a constant.
