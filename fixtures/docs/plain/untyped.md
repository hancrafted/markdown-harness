---
title: No type here
---

FAILS. The plain rule requires `type`, and nothing else. Under the old Floor
this file failed by construction, on a rule that named no fields at all; now it
fails because a rule asked.
