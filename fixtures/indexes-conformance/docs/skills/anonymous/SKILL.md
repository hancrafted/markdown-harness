---
name: anonymous
description: A skill addressed by its name, because it has no title.
---

# anonymous

`docs/skills/` overrides `descriptionSource` to `SKILL.md`, which is why this folder gets a
description in its parent's index at all. This folder holds no `index.md` and never will —
that is the shape the key exists for.
