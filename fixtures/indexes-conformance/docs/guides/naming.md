---
name: Naming things
description: Why a file name is a governed axis rather than an accident.
---

# Naming things

Carries `name` and no `title`, which is the shape every `SKILL.md` in this repo has. Without
`name` in the chain this entry would render as its file name and carry no information at all.
