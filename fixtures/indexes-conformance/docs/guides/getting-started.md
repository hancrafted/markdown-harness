---
title: Getting started
name: getting-started
description: The first thing to read, and the only one that assumes nothing.
---

# Getting started

Carries both `title` and `name`, so the link text resolves to the `title`. This is the live
tie that fixes the order of the chain rather than merely asserting it.
