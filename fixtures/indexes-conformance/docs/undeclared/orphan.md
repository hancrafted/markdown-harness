---
description: Declared nowhere, so listed nowhere.
---

# Orphan

`docs/undeclared/` is absent from `indexes-test-config.yaml`, so this document appears in no
index at all and its folder never appears in `docs/index.md`. No exclusion is needed — not
declaring it is the whole mechanism.

Nothing is reported about it either. A file no rule names is invisible, and an entry is not a
finding: `Governance is opt-in by path` holds for generation exactly as it holds for checking.
