---
description: Written down, not yet trusted, and excluded from the index by configuration.
---

# Draft page

`docs/reference/` carries `excludeFiles: [docs/reference/draft-*.md]`, so this file is absent
from `reference/index.md`. Because that key is non-empty, the region there carries one trailing
disclosure comment — a fact, not a list: no names and no count.
