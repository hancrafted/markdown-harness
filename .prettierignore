.archgate/rules.d.ts
node_modules
coverage
dist

# Vendored third-party text: this file IS the pin the OKF preset was derived
# from, so it must stay byte-identical to upstream. Formatting it would silently
# move the pin.
docs/okf/SPEC-v0.2.md

# Verbatim session transcripts, extracted mechanically from the session log.
# Reformatting one would silently alter the record it exists to preserve.
docs/workshop/**/raw.md

# Vendored material for repositories that do not exist yet. Formatting it here would
# apply this repo's style to files whose style is part of what a run is measured on.
.agents/skills/prepare-ablation-run/assets/**


# Conformance cases whose frontmatter block is deliberately defective -- a flow
# sequence that never closes, a fence that never closes, a block that parses to a
# list. Formatting would repair the exact defects they exist to present: prettier
# reads a lone `---` as a thematic break and inserts a blank line after it.
fixtures/conformance/docs/plain/broken/**

# The indexes suite's golden files: the exact bytes the generator must write,
# byte-for-byte, including the one case that is deliberately NOT a Prettier
# fixed point (the healed file keeps the blank line its deleted marker left
# behind). Formatting these would silently move the contract they are, and the
# suite would then assert prettier's opinion rather than the generator's output.
# The fixed-point property is checked IN the suite instead, against prettier's
# own API, so it is measured rather than assumed.
fixtures/indexes-conformance/expected/**

# One indexes Conformance case whose marker is deliberately unterminated. Per
# CommonMark that opens an HTML block running to the end of the document, so
# prettier reformats the rest of the file as raw HTML and destroys the exact
# defect the case exists to present. Its sibling `note.md` stays governed.
fixtures/indexes-conformance/docs/unterminated/index.md
