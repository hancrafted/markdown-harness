# Vision grilling session — raw transcript

A verbatim record of the `/grill-with-docs` session that produced `docs/vision/product.md`,
`docs/vision/architecture.md`, this repo's `README.md`, and issue #1. Kept as raw material for
workshop use.

| | |
| --- | --- |
| Session | `7e0752df-ae0c-4e82-a741-8f5ce6f93953` |
| Started | 2026-08-26T13:21:38Z |
| Ended | 2026-08-26T15:37:59Z |
| Duration | 2h 16m |
| Message records | 188 (main thread only; no sub-agent side-chains) |
| Human turns | 20 |

## What this file contains, exactly

**Included, byte-for-byte from the session log:** every human message and every assistant message,
in order, unedited — including the slash-command wrappers, injected system reminders and background
task notifications as they actually arrived.

**Represented as a one-line marker, not reproduced:** each tool invocation, shown as
`[tool] <name> — <description>` so no step is hidden.

**Excluded:** the 47 tool *results* (~118 KB — file contents, command output, and two long sub-agent
research reports), and 40 internal reasoning blocks. Both are recoverable from the session log above;
the sub-agent transcripts live in the sibling directory named for the session id.

Extracted mechanically with `jq`, never retyped — a reconstruction from memory would be a paraphrase
wearing the word "verbatim". This file is listed in `.prettierignore` for the same reason
`docs/okf/SPEC-v0.2.md` is: reformatting it would silently alter the record.


---

## Human  ·  2026-08-26T13:21:38.567Z

<local-command-caveat>Caveat: The messages below were generated by the user while running local commands. DO NOT respond to these messages or otherwise consider them in your response unless the user explicitly asks you to.</local-command-caveat>


---

## Human  ·  2026-08-26T13:21:38.558Z

<command-name>/clear</command-name>
            <command-message>clear</command-message>
            <command-args></command-args>


---

## Human  ·  2026-08-26T13:21:41.005Z

<local-command-caveat>Caveat: The messages below were generated by the user while running local commands. DO NOT respond to these messages or otherwise consider them in your response unless the user explicitly asks you to.</local-command-caveat>


---

## Human  ·  2026-08-26T13:21:41.002Z

<command-name>/effort</command-name>
            <command-message>effort</command-message>
            <command-args>max</command-args>


---

## Human  ·  2026-08-26T13:21:41.002Z

<local-command-stdout>Set effort level to max (this session only): Maximum capability with deepest reasoning. May use excessive tokens resulting in long response times or overthinking. Use sparingly for the hardest tasks.</local-command-stdout>


---

## Human  ·  2026-08-26T13:21:43.972Z

<command-message>grill-with-docs</command-message>
<command-name>/grill-with-docs</command-name>
<command-args>I want to define the product and architectural vision of this repository. I'm going to ramble a bit and I want in this session for you to help me set up some foundational documentation like the readme, the agents.md to help steer future sessions. Let's talk about the use case first. One personal and one potentially a business. Personally, I always wanted to set up a second brain, for example, using a LRM Wiki following Andrew Kaperthy. But my initial attempts sort of seen the markdown file degrade over time, especially if they were governed by like OpenClaw or Hermes. So I always stopped shy when there were so many documents I wasn't able to review.
 And it just exploded. And I lost the trust in the system. And this was a real problem. Second, I'm a startup coach and now start to work with certain accelerators here in my hometown where I'm able to deliver AI enablement training to startups and small and medium-sized companies. Obviously, the enablement mostly goes towards engineering teams, but I feel there's a huge market potential in bringing agentic workflows closer to non-technical staff and to have something that can help them govern knowledge work. Not necessarily centralized in some company vector database in a RAC pipeline, but really on a personal or small team level using a markdown repository as a collaborative medium. I have three to four workshops aligned in October to November, where if until then this initial product is ready, I can incorporate it into those workshops and have real feedback for product development. This is an opportunity I definitely want to use. An additional opportunity to expose this for testing and feedback is within my own company, where I also currently
 have a similar role to an AI enablement responsible. So the overall product vision is something a lightweight product. It can be a bit technical in the beginning. We don't need a full UI to make it really accessible to the most extreme non-technical people. But it should be something lightweight, configurable, and some sense of plug and play.
 For example, added to an obsidian bold, for example, to help it govern the markdown files there. The current floor as requirement, I would say, is a GenTIC CLI tool like Cloud Code and the Gravity or Codex and full admin right to their own local machine. I would say that is the current floor which I'm going for, which is also the requirement for those workshops. Now about the product itself. Obviously we are currently building a front metal harness based on the Google OKF standard, which is I feel currently the best standard out there, even though it's very early. And currently, I think we're building a governance layer, or maybe just a fancy word for a linter, that ensures some basic structures in the front matter section. Next step is to either develop some features that help automate some governance processes. So the first use case I'm always thinking is how to maintain a proper index MD for progressive disclosure. And one of the ideas I had is since we can configure the field description with hard rules like length, but also in
 intent on how to summarize it per path, per file, for example, then the index MD would be just a copy paste of the description file. And so we can have two jobs running like scheduled or mostly nightly. One that goes through the files and updates the stale description and the other one who then extracts the description and pastes it into the index file. That I think would be a already cool use case to have. In the long run, obviously, you can introduce same structured approaches for the markdown body. So, for example, like templates to use or even like in the config, which skills to use on a regular basis. So the config can also serve as a scheduler in the system. And lastly, since the config can be considered for deterministic checks, but also for agent steering, so like an MCP server or some script or skill base that basically extracts the important information for a target file or folder location and injects it into the prompt message so that it can be included on the file.
 runtime. Now to technical and architectural visions. I have over 10 years experience in developing JavaScript TypeScript and so obviously that is why we chose TypeScript as the programming language here. But the vision is that this tool should be usable also in a non-JavaScript environment so without Node.js installed. I have no experience in the other languages except from knowing them. So Python, Go, Rust, Java. Sowould wish that this tool that we are building can also be used there.But I wouldn't say it's a hard requirement right now. It's just something aered. because I believe any agentic workflows which has engineeringcomponent will have some programming language but also a lot of markdown files alongside it so if it's a pure Python project or pure backend Java project and this tools     should still be able to run checks and help them govern their markdown fileto the AI TypeScript Harness project, I want a full CI/CD pipeline set up in GitHub because this is where the single source of truth is. Local check run via Husky, that is already set up. Onto further architectural I would appreciate if you point out some load bearing decisions that we need to make now so that it doesn'tefactor or something later. - On the code base, I want to have deepmodules, something that Matt Pocock described, which enables easier agent traversal. see /codebase-design skill. Generally, we're going to follow the implement skill from   MedCoc, which is geared towards a red-green refactor. I just realized that ce goes a bit more towards operational and not vision. So let me rephrase the architectural vision. I would like to have a fast feedback loop set up so that agent can get feedback fast and often as well as offline, so locally with Husky as well asin the CICD pipeline. I would prefer as few dependencies as possible as wriry easy to maintain. And the more or the less dependency we have, theeasier it is maybe to migrate it later to another language if needed. For further references you can read this issue.                                                        https://github.com/hancrafted/okf-frontmatter-harness/issues/32
                                                                                                                                                                             ❯ /writing-for-agents I want to define the product and architectural visionoing to ramble a bit and I want in this session for you to help me set up some foundational documentation like the readme, the agents.md to help steer future sessions. Let's talk about the use case first. One personal and one potentially a        business. Personally, I always wanted to set up a second brain, for exampleng Andrew Kaperthy. But my initial attempts sort of seen the markdownfile degrade over time, especially if they were governed by like OpenClaw or Hermes. So I always stopped shy when there were so many documents I wasn't able to review.       And it just exploded. And I lost the trust in the system. And this was a r startup coach and now start to work with certain accelerators here in my hometown where I'm able to deliver AI enablement training to startups and small and medium-sized companies. Obviously, the enablement mostly goes towards engineering teams, but I feel there's a huge market potential in bringing agentic workflows clf and to have something that can help them govern knowledge work. Notnecessarily centralized in some company vector database in a RAC pipeline, but really on a personal or small team level using a markdown repository as a collaborative mediumI have three to four workshops aligned in October to November, where if untct is ready, I can incorporate it into those workshops and have realfeedback for product development. This is an opportunity I definitely want to use. An additional opportunity to expose this for testing and feedback is within my own companywhere I also currently
 have a similar role to an AI enablement responsible. So the overall product vision is something a lightweight product. It can be a bit technical in the beginning. We don't need a full UI to make it really accessible to the most extreme non-technice something lightweight, configurable, and some sense of plug and play.
 For example, added to an obsidian bold, for example, to help it govern the markdown files there. The current floor as requirement, I would say, is a GenTIC CLI tool like Cloud Code and the Gravity or Codex and full admin right to their own local is the current floor which I'm going for, which is also the requirementfor those workshops. Now about the product itself. Obviously we are currently building a front metal harness based on the Google OKF standard, which is I feel currently the best standard out there, even though it's very early. And currently, I thinnce layer, or maybe just a fancy word for a linter, that ensures somebasic structures in the front matter section. Next step is to either develop some features that help automate some governance processes. So the first use case I'm always thinking is how to maintain a proper index MD for progressive disclosure. Ais since we can configure the field description with hard rules likelength, but also in
 intent on how to summarize it per path, per file, for example, then the iny paste of the description file. And so we can have two jobs running like scheduled or mostly nightly. One that goes through the files and updates the stale description and the other one who then extracts the description and pastes it into the      index file. That I think would be a already cool use case to have. In the ln introduce same structured approaches for the markdown body. So, forexample, like templates to use or even like in the config, which skills to use on a regular basis. So the config can also serve as a scheduler in the system. And lastly,      since the config can be considered for deterministic checks, but also for aMCP server or some script or skill base that basically extracts theimportant information for a target file or folder location and injects it into the prompt message so that it can be included on the file.                                       runtime. Now to technical and architectural visions. I have over 10 years avaScript TypeScript and so obviously that is why we chose TypeScript asthe programming language here. But the vision is that this tool should be usable also in a non-JavaScript environment so without Node.js installed. I have no experience in    the other languages except from knowing them. So Python, Go, Rust, Java. Sowould wish that this tool that we are building can also be used there.But I wouldn't say it's a hard requirement right now. It's just something along the road to be considered. because I believe any agentic workflows which has engineering       component will have some programming language but also a lot of markdown fis a pure Python project or pure backend Java project and this toolsshould still be able to run checks and help them govern their markdown files in the project. Similar to the AI TypeScript Harness project, I want a full CI/CD pipeline set up in GitHub because this is where the single source of truth is. Local check eady set up. Onto further architectural I would appreciate if you pointout some load bearing decisions that we need to make now so that it doesn't cost if we want to do a refactor or something later. - On the code base, I want to have deep       modules, something that Matt Pocock described, which enables easier agent tsign skill. Generally, we're going to follow the implement skill fromMedCoc, which is geared towards a red-green refactor. I just realized that the previous couple sentence goes a bit more towards operational and not vision. So let me rephrase the architectural vision. I would like to have a fast feedback loop set up back fast and often as well as offline, so locally with Husky as well as in the CICD pipeline. I would prefer as few dependencies as possible as writing code with AI seems very easy to maintain. And the more or the less dependency we have, the easier it is maybe to migrate it later to another language if needed. For fread this issue.https://github.com/hancrafted/okf-frontmatter-harness/issues/32</command-args>


---

## Human  ·  2026-08-26T13:21:43.972Z

Base directory for this skill: /Users/han/Developer/markdown-harness/.claude/skills/grill-with-docs

Call the Skill tool twice, for "grilling" and "domain-modeling".


ARGUMENTS: I want to define the product and architectural vision of this repository. I'm going to ramble a bit and I want in this session for you to help me set up some foundational documentation like the readme, the agents.md to help steer future sessions. Let's talk about the use case first. One personal and one potentially a business. Personally, I always wanted to set up a second brain, for example, using a LRM Wiki following Andrew Kaperthy. But my initial attempts sort of seen the markdown file degrade over time, especially if they were governed by like OpenClaw or Hermes. So I always stopped shy when there were so many documents I wasn't able to review.
 And it just exploded. And I lost the trust in the system. And this was a real problem. Second, I'm a startup coach and now start to work with certain accelerators here in my hometown where I'm able to deliver AI enablement training to startups and small and medium-sized companies. Obviously, the enablement mostly goes towards engineering teams, but I feel there's a huge market potential in bringing agentic workflows closer to non-technical staff and to have something that can help them govern knowledge work. Not necessarily centralized in some company vector database in a RAC pipeline, but really on a personal or small team level using a markdown repository as a collaborative medium. I have three to four workshops aligned in October to November, where if until then this initial product is ready, I can incorporate it into those workshops and have real feedback for product development. This is an opportunity I definitely want to use. An additional opportunity to expose this for testing and feedback is within my own company, where I also currently
 have a similar role to an AI enablement responsible. So the overall product vision is something a lightweight product. It can be a bit technical in the beginning. We don't need a full UI to make it really accessible to the most extreme non-technical people. But it should be something lightweight, configurable, and some sense of plug and play.
 For example, added to an obsidian bold, for example, to help it govern the markdown files there. The current floor as requirement, I would say, is a GenTIC CLI tool like Cloud Code and the Gravity or Codex and full admin right to their own local machine. I would say that is the current floor which I'm going for, which is also the requirement for those workshops. Now about the product itself. Obviously we are currently building a front metal harness based on the Google OKF standard, which is I feel currently the best standard out there, even though it's very early. And currently, I think we're building a governance layer, or maybe just a fancy word for a linter, that ensures some basic structures in the front matter section. Next step is to either develop some features that help automate some governance processes. So the first use case I'm always thinking is how to maintain a proper index MD for progressive disclosure. And one of the ideas I had is since we can configure the field description with hard rules like length, but also in
 intent on how to summarize it per path, per file, for example, then the index MD would be just a copy paste of the description file. And so we can have two jobs running like scheduled or mostly nightly. One that goes through the files and updates the stale description and the other one who then extracts the description and pastes it into the index file. That I think would be a already cool use case to have. In the long run, obviously, you can introduce same structured approaches for the markdown body. So, for example, like templates to use or even like in the config, which skills to use on a regular basis. So the config can also serve as a scheduler in the system. And lastly, since the config can be considered for deterministic checks, but also for agent steering, so like an MCP server or some script or skill base that basically extracts the important information for a target file or folder location and injects it into the prompt message so that it can be included on the file.
 runtime. Now to technical and architectural visions. I have over 10 years experience in developing JavaScript TypeScript and so obviously that is why we chose TypeScript as the programming language here. But the vision is that this tool should be usable also in a non-JavaScript environment so without Node.js installed. I have no experience in the other languages except from knowing them. So Python, Go, Rust, Java. Sowould wish that this tool that we are building can also be used there.But I wouldn't say it's a hard requirement right now. It's just something aered. because I believe any agentic workflows which has engineeringcomponent will have some programming language but also a lot of markdown files alongside it so if it's a pure Python project or pure backend Java project and this tools     should still be able to run checks and help them govern their markdown fileto the AI TypeScript Harness project, I want a full CI/CD pipeline set up in GitHub because this is where the single source of truth is. Local check run via Husky, that is already set up. Onto further architectural I would appreciate if you point out some load bearing decisions that we need to make now so that it doesn'tefactor or something later. - On the code base, I want to have deepmodules, something that Matt Pocock described, which enables easier agent traversal. see /codebase-design skill. Generally, we're going to follow the implement skill from   MedCoc, which is geared towards a red-green refactor. I just realized that ce goes a bit more towards operational and not vision. So let me rephrase the architectural vision. I would like to have a fast feedback loop set up so that agent can get feedback fast and often as well as offline, so locally with Husky as well asin the CICD pipeline. I would prefer as few dependencies as possible as wriry easy to maintain. And the more or the less dependency we have, theeasier it is maybe to migrate it later to another language if needed. For further references you can read this issue.                                                        https://github.com/hancrafted/okf-frontmatter-harness/issues/32
                                                                                                                                                                             ❯ /writing-for-agents I want to define the product and architectural visionoing to ramble a bit and I want in this session for you to help me set up some foundational documentation like the readme, the agents.md to help steer future sessions. Let's talk about the use case first. One personal and one potentially a        business. Personally, I always wanted to set up a second brain, for exampleng Andrew Kaperthy. But my initial attempts sort of seen the markdownfile degrade over time, especially if they were governed by like OpenClaw or Hermes. So I always stopped shy when there were so many documents I wasn't able to review.       And it just exploded. And I lost the trust in the system. And this was a r startup coach and now start to work with certain accelerators here in my hometown where I'm able to deliver AI enablement training to startups and small and medium-sized companies. Obviously, the enablement mostly goes towards engineering teams, but I feel there's a huge market potential in bringing agentic workflows clf and to have something that can help them govern knowledge work. Notnecessarily centralized in some company vector database in a RAC pipeline, but really on a personal or small team level using a markdown repository as a collaborative mediumI have three to four workshops aligned in October to November, where if untct is ready, I can incorporate it into those workshops and have realfeedback for product development. This is an opportunity I definitely want to use. An additional opportunity to expose this for testing and feedback is within my own companywhere I also currently
 have a similar role to an AI enablement responsible. So the overall product vision is something a lightweight product. It can be a bit technical in the beginning. We don't need a full UI to make it really accessible to the most extreme non-technice something lightweight, configurable, and some sense of plug and play.
 For example, added to an obsidian bold, for example, to help it govern the markdown files there. The current floor as requirement, I would say, is a GenTIC CLI tool like Cloud Code and the Gravity or Codex and full admin right to their own local is the current floor which I'm going for, which is also the requirementfor those workshops. Now about the product itself. Obviously we are currently building a front metal harness based on the Google OKF standard, which is I feel currently the best standard out there, even though it's very early. And currently, I thinnce layer, or maybe just a fancy word for a linter, that ensures somebasic structures in the front matter section. Next step is to either develop some features that help automate some governance processes. So the first use case I'm always thinking is how to maintain a proper index MD for progressive disclosure. Ais since we can configure the field description with hard rules likelength, but also in
 intent on how to summarize it per path, per file, for example, then the iny paste of the description file. And so we can have two jobs running like scheduled or mostly nightly. One that goes through the files and updates the stale description and the other one who then extracts the description and pastes it into the      index file. That I think would be a already cool use case to have. In the ln introduce same structured approaches for the markdown body. So, forexample, like templates to use or even like in the config, which skills to use on a regular basis. So the config can also serve as a scheduler in the system. And lastly,      since the config can be considered for deterministic checks, but also for aMCP server or some script or skill base that basically extracts theimportant information for a target file or folder location and injects it into the prompt message so that it can be included on the file.                                       runtime. Now to technical and architectural visions. I have over 10 years avaScript TypeScript and so obviously that is why we chose TypeScript asthe programming language here. But the vision is that this tool should be usable also in a non-JavaScript environment so without Node.js installed. I have no experience in    the other languages except from knowing them. So Python, Go, Rust, Java. Sowould wish that this tool that we are building can also be used there.But I wouldn't say it's a hard requirement right now. It's just something along the road to be considered. because I believe any agentic workflows which has engineering       component will have some programming language but also a lot of markdown fis a pure Python project or pure backend Java project and this toolsshould still be able to run checks and help them govern their markdown files in the project. Similar to the AI TypeScript Harness project, I want a full CI/CD pipeline set up in GitHub because this is where the single source of truth is. Local check eady set up. Onto further architectural I would appreciate if you pointout some load bearing decisions that we need to make now so that it doesn't cost if we want to do a refactor or something later. - On the code base, I want to have deep       modules, something that Matt Pocock described, which enables easier agent tsign skill. Generally, we're going to follow the implement skill fromMedCoc, which is geared towards a red-green refactor. I just realized that the previous couple sentence goes a bit more towards operational and not vision. So let me rephrase the architectural vision. I would like to have a fast feedback loop set up back fast and often as well as offline, so locally with Husky as well as in the CICD pipeline. I would prefer as few dependencies as possible as writing code with AI seems very easy to maintain. And the more or the less dependency we have, the easier it is maybe to migrate it later to another language if needed. For fread this issue.https://github.com/hancrafted/okf-frontmatter-harness/issues/32


---

## Assistant  ·  2026-08-26T13:21:52.169Z

I'll start by loading both skills as instructed, then read the referenced issue and the repo's current state before I start grilling you.


---

## Assistant  ·  2026-08-26T13:21:53.140Z

`[tool] Skill`


---

## Assistant  ·  2026-08-26T13:21:53.157Z

`[tool] Skill`


---

## Human  ·  2026-08-26T13:21:53.151Z

Base directory for this skill: /Users/han/Developer/markdown-harness/.claude/skills/grilling

Interview the user relentlessly until you reach a shared understanding. Map this as a **design tree**: every decision branches into the decisions that hang off it.

Work the tree in **rounds**. The **frontier** is every decision whose prerequisites are already settled: the questions you can ask _now_ without guessing at answers you haven't heard yet. Ask the whole frontier in one round: number each question and give your recommended answer. Then wait for the user's answers before the next round.

Format a round like so:

```
❓ **Q1** - **<question title>**: <question body, might be multiple paragraphs, including multiple choices>

➡️ <your recommended answer>

---

❓ **Q2** - **<question title>**: <question body, might be multiple paragraphs, including multiple choices>

➡️ <your recommended answer>
```

Each round the user answers reshapes the tree: settled decisions push the frontier outward and unblock questions that depended on them. Recompute the frontier and ask the next round. A question whose answer depends on another question still open in this round belongs to a _later_ round, not this one.

Finding _facts_ is your job, never the user's. When a frontier question needs a fact from the environment (filesystem, tools, etc.), dispatch a sub-agent to find it; don't ask the user for anything you could look up yourself. Don't block on it: a running exploration is an unsettled prerequisite, so only the questions downstream of it wait for the sub-agent to report; ask the rest of the frontier now. The _decisions_ are the user's: put each to them and wait.

The session is done when the frontier is empty: every branch of the design tree visited, nothing left silently assumed. Do not act on it until the user confirms you have reached a shared understanding.



---

## Human  ·  2026-08-26T13:21:53.223Z

Base directory for this skill: /Users/han/Developer/markdown-harness/.claude/skills/domain-modeling

# Domain Modeling

Actively build and sharpen the project's domain model as you design. This is the _active_ discipline: challenging terms, inventing edge-case scenarios, and writing the glossary and decisions down the moment they crystallise. (Merely _reading_ `CONTEXT.md` for vocabulary is not this skill: that's a one-line habit any skill can do. This skill is for when you're changing the model, not just consuming it.)

## File structure

Most repos have a single context:

```
/
├── CONTEXT.md
├── docs/
│   └── adr/
│       ├── 0001-event-sourced-orders.md
│       └── 0002-postgres-for-write-model.md
└── src/
```

If a `CONTEXT-MAP.md` exists at the root, the repo has multiple contexts. The map points to where each one lives:

```
/
├── CONTEXT-MAP.md
├── docs/
│   └── adr/                          ← system-wide decisions
├── src/
│   ├── ordering/
│   │   ├── CONTEXT.md
│   │   └── docs/adr/                 ← context-specific decisions
│   └── billing/
│       ├── CONTEXT.md
│       └── docs/adr/
```

Create files lazily: only when you have something to write. If no `CONTEXT.md` exists, create one when the first term is resolved. If no `docs/adr/` exists, create it when the first ADR is needed.

## During the session

### Challenge against the glossary

When the user uses a term that conflicts with the existing language in `CONTEXT.md`, call it out immediately. "Your glossary defines 'cancellation' as X, but you seem to mean Y. Which is it?"

### Sharpen fuzzy language

When the user uses vague or overloaded terms, propose a precise canonical term. "You're saying 'account': do you mean the Customer or the User? Those are different things."

### Discuss concrete scenarios

When domain relationships are being discussed, stress-test them with specific scenarios. Invent scenarios that probe edge cases and force the user to be precise about the boundaries between concepts.

### Cross-reference with code

When the user states how something works, check whether the code agrees. If you find a contradiction, surface it: "Your code cancels entire Orders, but you just said partial cancellation is possible. Which is right?"

### Update CONTEXT.md inline

When a term is resolved, update `CONTEXT.md` right there. Don't batch these up: capture them as they happen. Use the format in [CONTEXT-FORMAT.md](./CONTEXT-FORMAT.md).

`CONTEXT.md` should be totally devoid of implementation details. Do not treat `CONTEXT.md` as a spec, a scratch pad, or a repository for implementation decisions. It is a glossary and nothing else.

### Offer ADRs sparingly

Only offer to create an ADR when all three are true:

1. **Hard to reverse**: the cost of changing your mind later is meaningful
2. **Surprising without context**: a future reader will wonder "why did they do it this way?"
3. **The result of a real trade-off**: there were genuine alternatives and you picked one for specific reasons

If any of the three is missing, skip the ADR. Use the format in [ADR-FORMAT.md](./ADR-FORMAT.md).



---

## Assistant  ·  2026-08-26T13:22:00.804Z

`[tool] Bash — List repo root and file tree`


---

## Assistant  ·  2026-08-26T13:22:01.664Z

`[tool] Bash — Read referenced GitHub issue 32`


---

## Assistant  ·  2026-08-26T13:22:08.450Z

`[tool] Bash — Read foundational docs and config`


---

## Assistant  ·  2026-08-26T13:22:09.205Z

`[tool] Bash — Read issue 32 body`


---

## Assistant  ·  2026-08-26T13:22:17.409Z

`[tool] Bash — Read config contract source`


---

## Assistant  ·  2026-08-26T13:22:19.695Z

`[tool] Bash — Read design-ADR, OKF readme, CI, hooks, fixture config`


---

## Assistant  ·  2026-08-26T13:24:16.106Z

I've read the repo and issue #32. Two focused explorations before I put questions to you — I'd rather not re-ask something the harvested research already settles.


---

## Assistant  ·  2026-08-26T13:24:26.625Z

`[tool] Agent — Survey harvested research for settled decisions`


---

## Assistant  ·  2026-08-26T13:24:36.421Z

`[tool] Agent — Survey predecessor repos for vision + README precedent`


---

## Assistant  ·  2026-08-26T13:26:21.134Z

Read: `AGENTS.md`, `CONTEXT.md`, `docs/agents/domain.md`, `src/config/{contract,constraints}.ts`, `docs/design-adr/0001`, `docs/okf/README.md`, `fixtures/valid-test-config.yaml`, the husky + CI setup, and issue #32 in full including both completion comments. Two explorations are running in the background (harvested research; predecessor-repo vision + README precedent) — they feed later rounds, not this one.

**Three things in your ramble collide with vocabulary this repo has already settled.** Naming them now because they change how the questions below are framed:

1. **"a governance layer, or maybe just a fancy word for a linter"** — `CONTEXT.md` already refuses that equivalence. A linter answers *"is this file wrong?"* after the file exists. **Steering query** is defined as asking *"what governs this path?"* **before** it's written, and issue #32 calls that the headline feature and the whole argument for a CLI. If the README opens with "linter" you are selling the part that already has ten free competitors.

2. **"the config can also serve as a scheduler"** — collides head-on with a settled definition. **Core** "owns the config file and hands each Module its section"; a **Module** is "a named checking domain". A scheduler is neither. Either scheduling is a Module with its own top-level section (and then "checking domain" is wrong in the glossary), or it lives outside the config. Unresolved — I've queued it for round 2.

3. **"index.md would be just a copy-paste of the description"** — this needs a word the glossary doesn't have. **Governed file** means *matched by a Rule*; it says nothing about who **authored** the bytes. A generated index is a file whose content the Harness owns. Round 2 names it.

---

❓ **Q1** — **The promise**: What is the first sentence of the README? Not the feature list — the sentence that says which pain this removes. Three candidates, and they are not compatible; each makes a different thing the product and the other two supporting acts.

**(a) Conformance** — *"your markdown is structurally machine-readable."* Mechanism: check. Sells a linter.
**(b) Steering** — *"an agent knows what to write before it writes it."* Mechanism: the steering query. Docs are born conformant rather than repaired.
**(c) Trust budget** — *"every document says where it came from and how far it can be trusted, and the index stays small enough that you actually review it."* Mechanism: OKF's provenance fields (`generated.by`, `sources[]`, `verified[]`, `stale_after`) plus a derived index.

Here's why I think this matters more than it looks: your personal story and your workshop story are **the same story only under (c)**. What killed your earlier vaults wasn't malformed YAML, it was volume past your review capacity and no way to tell which documents still deserved trust. Under (a) the two stories diverge — a non-technical knowledge worker does not care whether `sources[]` is a list of mappings. Under (c) they're one product with one thesis, and the frontmatter harness becomes the *enforcement mechanism* for a promise about trust rather than the product itself.

➡️ **(c) is the promise; (b) is the mechanism that makes (c) survivable; (a) is the floor that makes (b) possible.** The README's first sentence is (c) and never mentions YAML. `CONTEXT.md`'s current opening line is (a)+(b) — accurate for the engineering repo, wrong for the product's front door.

---

❓ **Q2** — **Who v1 is for, and whether git is assumed**: Your floor requirement is "an agentic CLI plus admin rights on their own machine." That is a statement about the *agent*, not about the *repo* — and it quietly leaves open whether the Harness may assume **git**. This is a fork, not a detail: staleness detection, the config-loosening check issue #32 calls "the honest mechanism", and the whole CI story all want git. An Obsidian vault on a workshop participant's laptop may have none.

**(a)** Git-backed repo is the only supported case. Vault users initialise a repo — it's one command.
**(b)** The filesystem is the only assumption in the core path. Git is a *probed capability*: `check` and `steer` never require it; git-derived features degrade to a stated fallback or switch off with a printed reason.
**(c)** Vault-first: no git, no CI, no npm — a downloadable binary and a config file.

➡️ **(b).** The acid test is a Windows laptop in a room of twenty people: if `mh check` shells out to git and you find out live, the workshop is the demo. But refusing git outright throws away your own CI story and the anti-loosening check. So: **no operation on the check-or-steer path may require git, and every git-dependent feature must state in its output that it is degraded.** Ship (a) as the *documented happy path*, hold (b) as the *hard architectural rule*.

---

❓ **Q3** — **Does the Harness write files, and where is the seam?**: Your two nightly jobs are not one system. "Extract the descriptions and paste them into `index.md`" is a **deterministic projection** of data already read. "Go through the files and update the stale description" is an act of **judgment** that no amount of config can make deterministic. Bundling them as "two jobs" hides the most load-bearing seam in the product.

**(a) Read-only forever.** The Harness reports; an agent does every write.
**(b) Derived-only writes.** The Harness writes *only* artifacts it wholly owns and can regenerate from scratch — a generated index — and never touches a hand-authored file. Anything requiring judgment is *emitted as work*, never performed.
**(c) `--fix` anywhere**, including rewriting prose fields.

➡️ **(b), stated as a rule with teeth: the Harness writes only what it can derive from data it has already read.** Regeneration must be byte-identical given identical input, or the nightly job produces a diff every night and you stop reading the diffs — which is exactly the failure mode that lost your trust the first time. (c) is how a governance tool becomes untrustworthy: the moment it can rewrite `description`, its output stops being evidence about your repo and becomes evidence about itself. This decision also forces two sub-questions I'm holding for round 2: whether ownership is whole-file or a marked region, and what the term for such a file is.

---

❓ **Q4** — **Who runs the model, and what does the Harness hand it?**: Given Q3, "refresh the stale descriptions" has to happen *somewhere*. Three places it could live:

**(a) Nowhere formal** — the report is prose; a human or agent reads it and figures it out.
**(b) A work order.** The Harness emits a machine-readable list: this path, this field, this `intent` from the winning Rule, this constraint it currently fails. An agentic CLI consumes it, writes, and the Harness re-checks. The `intent` field you already made mandatory is *precisely* the payload this needs — that is what it was for.
**(c) The Harness calls a model itself** — ships a provider, a model choice, an API key, and a bill.

➡️ **(b), and (c) never.** (c) asks twenty workshop participants to configure an API provider before they see any value, puts a variable-cost dependency inside a governance tool, and destroys determinism — the same repo would check differently on Tuesday. Under (b), your stated floor requirement stops being an incidental prerequisite and becomes **a documented architectural dependency**: markdown-harness is deliberately the deterministic half of a two-part system whose other half is the user's own agent. That's a strength to state loudly, not a limitation to apologise for. It's also what makes "which skills to run regularly" expressible at all — the config names *work*, the agent performs it.

---

❓ **Q5** — **Portability: what exactly is the portable thing?**: You framed "usable without Node" as a *language* risk. I think that's the wrong axis and it's worth arguing about, because getting it wrong costs you a rewrite you don't need.

A Java shop does not want to *port* your tool. They want to *run* it without installing Node. Those are different problems with different fixes: the first is a language choice, the second is a **packaging** choice — `bun build --compile`, `deno compile` or Node SEA give you one self-contained executable per platform, no runtime installed. So TypeScript is not a threat to the stated vision *provided* nothing Node-shaped leaks into the observable contract.

What *is* expensive to retrofit is the thing that makes any reimplementation verifiable:

**(a)** Node/npm only. Port by rewriting and hoping.
**(b)** The **specified artifact** is the config language + the report format + the fixture corpus with expected reports. The code is one implementation of it. A port is *verified against the corpus*, not reverse-engineered from source.
**(c)** Ship single-file executables now.
**(d)** Write it in Go or Rust now.

➡️ **(b) now, (c) when the first non-Node adopter actually appears, (d) never.** (b) is nearly free today — you already have `fixtures/` (15 files) and 70 tests; the work is *declaring them the spec* and freezing the report format, which is a decision, not a project. Do it after the checker matures and you're archaeology-ing intent out of assertions. Note the immediate consequence: it makes **glob semantics and report shape part of your public contract**, which is why Q6 matters.

---

❓ **Q6** — **The dependency budget, and where MCP lives**: "As few dependencies as possible" is a preference until it's a number. Right now you have exactly one non-dev runtime candidate (`yaml`) — the cheapest moment there will ever be to make it a rule.

**(a)** Soft preference, decide case by case.
**(b) Hard rule: one runtime dependency, `yaml`.** Glob matching, the reporter, and the MCP server are hand-rolled on `node:` builtins. Dev dependencies unconstrained.
**(c) Zero runtime dependencies** — hand-roll a YAML subset too.

And the specific case that forces the answer: the **MCP server**. The official SDK is a substantial tree that would dominate the budget and put a dependency graph between you and a single-file binary. MCP over stdio is JSON-RPC 2.0 framed on newline-delimited JSON — genuinely small, and hand-rolling it keeps the steering surface reimplementable in any language.

➡️ **(b), with MCP hand-rolled and served from the same binary behind `mh mcp`.** Reject (c): YAML 1.2 is the one thing here you cannot hand-roll correctly, and `yaml` buys correctness you'd otherwise pay for in silent misparses — the worst possible failure mode for a trust tool. But be clear-eyed about (b)'s real cost: **you now own glob semantics** — `**` behaviour, case sensitivity, Windows separators, whether a leading `!` means anything. `docs/research/pathrule-precedence.md` is 56K because that's genuinely hard. I'd argue you have to own it anyway the moment Q5 says the config language is the spec, so this is a cost you were already paying.

---

Held for later rounds, so you know I haven't dropped them: where scheduling lives (the Q2 collision) · the term and ownership model for generated files · severity tiers, and whether "37 stale descriptions" is an error, a warning, or neither · suppression — how an adopter silences one violation without deleting the Rule · the report format's exact shape · the config filename (issue #32 lists it as unconfirmed and nothing pins it yet) · what must be true by October for a workshop, and what gets cut · the CI/CD gates and whether this publishes to npm · and finally which documents this session actually writes.


---

## Human  ·  2026-08-26T13:27:04.561Z

<task-notification>
<task-id>a30c107ca939b2892</task-id>
<tool-use-id>toolu_01Jddv9jQS2cxEzsGhdYdEMS</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-han-Developer-markdown-harness/2a14b75a-1112-4713-bd5f-935b92ac85c3/tasks/a30c107ca939b2892.output</output-file>
<status>completed</status>
<summary>Agent "Survey predecessor repos for vision + README precedent" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result>## 1. Vision statements that already exist in writing

**Neither `okf-frontmatter-harness` nor the current `markdown-harness` has a README.** No README.md at either repo root (repo A root: `AGENTS.md`, `CLAUDE.md`, `CONTEXT.md`, `docs/`, no README). Every vision statement lives in `CONTEXT.md` openers and issue bodies.

**A. The current one-liner — `/Users/han/Developer/markdown-harness/CONTEXT.md` lines 1-5, verbatim:**

&gt; # markdown-harness
&gt;
&gt; A CLI and MCP steering API that reads one config, checks every governed markdown file, and answers "what governs this path?" for an agent about to write one. `frontmatter-harness` is the first module; OKF ships as a preset config rather than as behaviour.

**B. The predecessor's one-liner — repo A `CONTEXT.md` opener, verbatim:**

&gt; # okf-frontmatter-harness
&gt;
&gt; A governance harness for agentic markdown repos. It implements Google's Open Knowledge Format v0.2 as an interface: OKF names the frontmatter vocabulary, the harness supplies configurable deterministic checks for the fields OKF proposes and stays permissive toward everything else.

**C. The product argument — repo A issue #32 ("Rebuild as markdown-harness"), `### Outcome`, verbatim:**

&gt; **`markdown-harness`**: a CLI plus MCP steering API that reads one config, checks all governed files, and answers "what governs this path?" for an agent about to write. `frontmatter-harness` is the first module. **OKF v0.2 ships as a default preset, not built-in behaviour.**
&gt;
&gt; archgate is retained but **internal only** — dev governance over `markdown-harness`'s own source. Never shipped, never carrying OKF rules.

Plus the headline-feature claim, verbatim: *"**#18 — "A steering API: what governs this path, before the file is written" — was deferred because archgate structurally cannot do it. It is now the product's headline feature.** That is the whole argument for a CLI."*

**D. Adoption-posture lines worth reusing as README copy** (repo A issue #1, standing preferences 2, 3, 5, 8):

- *"Every decision must survive being copied into a foreign repo unchanged."*
- *"Governance is opt-in. Only paths named by a rule are governed (default-ignore), because a harness that reports 200 violations on install gets uninstalled. Ship a *default ruleset* users can delete or adjust."*
- *"Composition is single winner, first match wins — no merging, no inheritance… **Authoring discipline: most specific rules first, broadest last**"*
- *"All rules run at `error`. Config is the softness dial — a check you didn't enable doesn't run."*

**E. Positioning line, repo A issue #1 preference 13:** *"This harness is in the "path-attached settings" family, not the "path-attached schema" family… Say so in the ADR — "frontmatter schema" is what a reader will otherwise assume."* — Directly a README obligation: readers will assume "frontmatter schema" unless told otherwise.

**F. Measured perf numbers already public** (issue #32): ~150ms end-to-end over 1000 markdown files, 10k ≈ 1.1s, YAML parse is 80% of cost. Ready-made README credibility line.

Also: `/Users/han/Developer/markdown-harness/docs/okf/README.md` (a doc README, not a product one) carries the "**This file is the pin**" argument — the RFC/W3C-immutability reasoning for vendoring the spec instead of pinning a version.

## 2. What #1 and #15 still want

**#1 — "Map: OKF frontmatter governance harness, self-hosted"** (open, label `wayfinder:map`, 1 comment)

This is the wayfinder map — a living index, not a work item. Its stated Destination is the *old* one: `okf-config.yml` + `OKF-001-config` + `OKF-002-frontmatter` ADRs with companion `.rules.ts`, and `archgate check` green over this repo's markdown. Its single comment collapses execution into #20 — and **#20 is now superseded by #32**, which explicitly says #20 is *"Superseded. Not harvested."* So the map's Destination is stale by design; what it still supplies is the **decision record**: 17 standing preferences, one-line resolutions for 15 closed tickets, a `Not yet specified` list (3 items) and an `Out of scope` list (9 items). Retained because it is the only place the *why* behind every settled decision is written down.

Vision-relevant items still live inside it:
- The **northstar it names out of scope**: *"npm packaging and `npx` distribution — the bin entry, target-repo detection, archgate bootstrap, file copying, idempotent re-runs. **The northstar**, and a second effort that depends on a proven local harness."*
- Publishing to `archgate/awesome-adrs` as an ADR-Pack (out of scope, depends on ADRs existing).
- Frontmatter↔body coherence — still in scope for the destination; blocker is *vocabulary, not mechanism*.
- Note: the Floor and `types:` machinery that preferences 6, 8, 17 build up are **removed** by #32 ("The Floor. **Removed.** No `types:` property; `type` is an ordinary field"). Read #1's preferences through #32's decisions table.

**#15 — "Detect when upstream OKF drifts from the pinned revision"** (open, 0 comments)

A deferred feature request, kicked out of scope by #13. Wants a drift watcher. Still asks for exactly:

1. Fetch `SPEC.md` from `GoogleCloudPlatform/open-knowledge-format@main`.
2. Compare against the vendored `docs/okf/SPEC-v0.2.md`.
3. On mismatch, **print the diff** — *"the diff is the point, since it is the input Reconciliation needs."*

Constraints it says any solution inherits: cannot be an archgate rule (GEN-001 §7 permits only `error`, and upstream moving is not the committer's error); must not go in `verify` (network call per-commit is offline-hostile and an adoption tax); Renovate/Dependabot can't do it (upstream publishes **0 tags, 0 releases**, verified 2026-08-24). Desired shape: an isolated `npm run okf:drift` that blocks nothing, optionally a weekly cron. Key correction it preserves: *"A checksum over the vendored copy detects **local tampering, not upstream drift**. Only a fetch detects upstream."*

Motivating incident, worth a README sentence: upstream PR #323 changed `stale_after`, `last_modified`, `usage_window` from `YYYY-MM-DD` dates to full ISO 8601 datetimes **under an unchanged `0.2` label**.

Third open issue: **#32** — the live rebuild spec. Phase 0 done (typescript-ai-harness v0.1.5), Commit 1 done. Remaining per its tail: *"Commit 3+: config reader, resolver, check command, MCP steering API, OKF preset."*

## 3. `typescript-ai-harness` README — structural outline

277 lines, ~2,026 words. Source: `https://github.com/hancrafted/typescript-ai-harness/blob/main/README.md`.

**It opens with YAML frontmatter** (`type: docs`, `title`, `description`) — the README is itself a governed OKF-shaped document. Strong precedent: a frontmatter-governance tool's README should absolutely do this, and should be checked by its own tool.

| # | Section | Lines | What it does |
|---|---|---|---|
| — | frontmatter | 1-5 | `type/title/description` |
| 1 | `# typescript-ai-harness` + 3 paragraphs | 7-23 | 2-sentence what-it-is, then a paragraph naming every tool it stands up, then **5 bolded one-line selling points** (`**One command, no install step**`, `**Pick what you want**`, `**Safe to re-run**`, `**Preview first**`, `**Scriptable**`) |
| 2 | `## Requirements` | 25-34 | Node 24+, npm, git — plus a paragraph pre-empting "do I need tsx?" |
| 3 | `## How to use` | 36-112 | Numbered walkthrough `### 1.`→`### 4.`, then `### Preview without changing anything`, `### Non-interactive mode`, `### Re-running = update` |
| 4 | `## What gets written` | 114-158 | 3 tables (paths × on-re-run; scripts × provider × command) + `### Commit verification model` prose |
| 5 | `## Notes &amp; current limitations` | 160-175 | Honest bullet list: npm only, one ADR copied, what's deferred |
| 6 | `## Development` | 177-273 | Contributor half: script list, `### Layout` (ASCII tree), `### Adding to the harness`, `### How it's tested`, `### CI/CD pipeline` (table), `### Releasing` |
| 7 | `## License` | 275-277 | `MIT`, one line |

**Observations**

- **No "why this exists" section, no comparison/alternatives section, no badges, no ToC.** The *why* is compressed into one clause of the first sentence: *"so you stop re-deriving the same boilerplate by hand."* That is the entire motivation copy.
- **No install section** — deliberately: `npx`, no install step, and that is sold as a feature in bullet 1.
- **Quickstart is 3 lines deep in `## How to use` step 1**, not at the top. The first runnable command appears at line ~42, after ~35 lines of prose. Slow for a tool people are evaluating.
- **Tone: engineer-to-engineer, terse, zero marketing.** No "blazing fast", no emoji headings, no "Why you'll love it". Assumes a Node dev who knows what Knip and lint-staged are and never explains them.
- **Tables carry all reference data** (5 tables: integrations, files written, scripts, CI workflows). Prose carries only rationale. Consistent and easy to skim.
- **Addressed to humans running a CLI**, not to agents. "AI" in the name is never explained in the README; there is no agent-facing section, no MCP, no "for agents" block. The only agent touch is one clause about a `.claude/rules/` symlink *"that loads the contract into agent context."*
- **Bimodal document**: lines 1-175 are for users, 177-273 are for contributors of *this* repo. The seam at `## Development` is clean, but the second half is &gt;35% of the doc — release plumbing, OIDC trusted publishing, and a three-workflow CI rationale that a user will never need.
- **Strongest structural move to copy: `## Notes &amp; current limitations`.** Plainly states what does not work (`npm only`, `Frontmatter governance is **not** shipped in this version`) and what is deferred. That builds trust cheaply and is exactly where a governance tool should confess its escape hatches (e.g. archgate's suppression comment defeating "unrelaxable", already conceded in repo A's `CONTEXT.md`).
- **Second strongest: `### Re-running = update` and the overwrite/merge/append table.** For a tool that writes into someone else's repo, spelling out per-path what is overwritten vs surgically merged vs appended is the section that gets it adopted. A linting tool aimed at non-technical users needs the equivalent: what it will and won't touch in your files.

**For a governance/linting README aimed at engineers *and* non-technical knowledge workers — what to copy vs avoid**

Copy: frontmatter on the README itself (dogfooding); the 5-bullet bolded value prop; the honest limitations section; the "what gets written / what gets touched" table; the tables-for-reference/prose-for-rationale split; terse engineer register; one-line License.

Avoid or invert:
- **Move the quickstart to line ~10.** Do not bury the first command 35 lines down for a mixed audience.
- **Add the missing "why this exists" section.** One clause is not enough when the reader may not accept that markdown frontmatter needs governing at all. The material already exists (issue #1 preference 3's "a harness that reports 200 violations on install gets uninstalled", the 150ms perf numbers, the "path-attached settings, not path-attached schema" positioning).
- **Split the audiences explicitly.** `typescript-ai-harness` assumes one persona and gets away with it. A tool for both engineers and knowledge workers cannot — either two top-level tracks or a separate contributor doc, and either way `## Development` should not be 35% of the README.
- **Do not assume vocabulary.** `typescript-ai-harness` never defines Knip, lint-staged, or ADR. "Frontmatter", "governed file", "rule", "preset", "steering" all need a one-line gloss for a non-technical reader — and repo A's `CONTEXT.md` glossary is the ready source.
- **Add the comparison section it lacks.** Issue #3's finding — *"No surveyed tool combines data config + path scoping + a constraint vocabulary + relaxation"* — is the differentiator and belongs in the README, not just in `docs/research/frontmatter-linter-config.md`.
- **Give the headline feature its own section.** The steering API ("what governs this path, before the file is written") is #32's stated whole-argument-for-a-CLI; `typescript-ai-harness`'s README has no equivalent single-feature spotlight to copy from, so this needs inventing.</result>
<usage><subagent_tokens>58078</subagent_tokens><tool_uses>25</tool_uses><duration_ms>148122</duration_ms></usage>
</task-notification>


---

## Assistant  ·  2026-08-26T13:27:34.936Z

Background exploration #2 came back (the research survey is still running). Four findings that sharpen the round above — **no new questions, Round 1 still stands as asked.**

**1. Q1 gains a positioning obligation, already written down.** Predecessor issue #1, preference 13: *"This harness is in the 'path-attached settings' family, not the 'path-attached schema' family… 'frontmatter schema' is what a reader will otherwise assume."* And issue #3's survey finding — *"no surveyed tool combines data config + path scoping + a constraint vocabulary + relaxation"* — is the actual differentiator, currently buried in `docs/research/`. Both belong in the README's first screen. This strengthens my (c) recommendation: a *schema* validator is what (a) sells, and it's the wrong family.

**2. Q1 also has a hard adoption constraint attached.** Preference 3: *"a harness that reports 200 violations on install gets uninstalled."* That is why **Preset** is defined the way it is, and it means the trust promise has to be true on day one of an existing 400-file vault — not just on a greenfield one. Worth holding against whatever you answer.

**3. A queued question may already be closed — and it collides with Q4.** Preference 5: *"All rules run at `error`. Config is the softness dial — a check you didn't enable doesn't run."* Clean, and I'd keep it. But a **work order** ("37 descriptions are stale") is not a violation the committer caused, so under an error-only model it cannot be a Rule at all. Two ways out, and the answer to Q4 picks one: either staleness lives outside the Rule system entirely (a separate command, blocking nothing), or the error-only preference breaks. I'd take the first — it keeps preference 5 intact and matches finding 4 below.

**4. Q2's git rule wants a sibling: no network on the check path.** Issue #15 (the OKF drift watcher, still open) rejects putting a fetch in `verify` as *"offline-hostile and an adoption tax"* — and wants an isolated command that blocks nothing. That's the same shape as the staleness job, and it generalises: **`check` and `steer` touch neither git nor the network; anything that does is a separate command that cannot fail a commit.** Your stated architectural vision — fast, frequent, offline feedback — is already this rule, unnamed.

Also confirmed: **neither repo has ever had a README.** Everything above lives in `CONTEXT.md` openers and issue bodies. And `typescript-ai-harness`'s README carries its own OKF frontmatter — a dogfooding precedent worth inheriting when we get to which documents this session writes.

Still waiting on your Round 1 answers — **Q1** promise, **Q2** audience + git, **Q3** writes, **Q4** who runs the model, **Q5** portable artifact, **Q6** dependency budget.


---

## Human  ·  2026-08-26T13:30:33.578Z

<task-notification>
<task-id>aac81a3ba10091488</task-id>
<tool-use-id>toolu_017XZTaKVnvvtxFGbkRv6AyB</tool-use-id>
<output-file>/private/tmp/claude-501/-Users-han-Developer-markdown-harness/2a14b75a-1112-4713-bd5f-935b92ac85c3/tasks/aac81a3ba10091488.output</output-file>
<status>completed</status>
<summary>Agent "Survey harvested research for settled decisions" finished</summary>
<note>A task-notification fires each time this agent stops with no live background children of its own. The user can send it another message and resume it, so the same task-id may notify more than once.</note>
<result># Research Brief: `docs/research/` (7 files, read in full)

Citations are `file §section`. All paths under `/Users/han/Developer/markdown-harness/docs/research/`.

**Read before item 1:** the single largest thing these files get wrong is not in your known-stale list. `CONTEXT.md` ("Rule") now says *"For any file the **first matching Rule** is the complete set of Constraints that applies — nothing merges and nothing is inherited."* Every merge / cascade / relaxation conclusion below is settled **about a design this repo no longer has**. Flagged in item 3; item 1 marks the affected bullets `[CASCADE]`.

---

## 1. Already settled

### (a) Config language / schema vocabulary

- **Config is data, not code.** `frontmatter-linter-config.md §3.6, §4` + `parts/astro-vitepress.md §1.5, §3.7`: Astro RFC-0027's case for Zod is 2-of-3 Astro-internal ("takes the maintenance burden off of Astro's shoulders"); Astro must then project Zod back to JSON Schema for editors, lossily (`unrepresentable: 'any'`); Docusaurus is where the code path terminates (hardcoded Joi, no config surface at all). "**Decap is the counter-proof that data scales.**"
- **"Just use JSON Schema" is closed.** `frontmatter-linter-config.md §1, §7`: Core §10.2 normative *MUST NOT* impact sibling subschemas; `allOf` is pure conjunction; draft-03 `extends` was also conjunctive; no `unevaluatedRequired`/`notRequired`/`optional`. Plus `frontmatter-key-order.md §0(c), §1.3` — JSON Schema *structurally cannot* express order; `propertyOrder` was redirected out of the spec twice.
- **Vocabulary shape: positive, closed, labelled enumeration + co-located intent.** Only Decap passes the "author correct frontmatter from the config cold" test, and for a structural reason (whitelist with labels, not a blacklist of prohibitions). `frontmatter-linter-config.md §4`; `parts/docusaurus-decap.md` §"Could an agent author…".
- **Generic option names transfer directly** — `required`, `pattern`, `min`, `max`, `options`, `default`, `multiple`, `fields`. `parts/docusaurus-decap.md` §Implications 4. *(But see item 3 — "never type-aware" is now false.)*
- **`intent:` must be mandatory and echoed verbatim in the violation message.** Four-for-four negative result; Decap is the only tool that renders guidance *at* the error (`hint` turns red) and still misses, because `hint` is `{type:'string'}` and the mandatory key list is `required:['name']` — nothing checks a hint exists. `frontmatter-linter-config.md §3.3` ("the survey's clearest free win"); `parts/docusaurus-decap.md` §"`hint:` and `label:`"; `parts/astro-vitepress.md` §Implications 5; `parts/markdownlint-obsidian.md §1.4`.
- **Default-ignore via `"default": false` is established prior art** — markdownlint has exactly the key and defaults it the wrong way ("When no configuration object is passed… all rules are enabled"). *Keep the key, flip the default.* `frontmatter-linter-config.md §5`; `parts/markdownlint-obsidian.md §1.3` + Implication 6.
- **Report, never rewrite.** Stated twice as an explicit ADR instruction. `frontmatter-linter-config.md §5`; `parts/markdownlint-obsidian.md §2.1` + Implication 10.
- **One address per thing; one config-file kind; one precedence rule.** markdownlint lets `MD010` / `no-hard-tabs` / tag `whitespace` all key the same flat map, last-key-wins — correctness depends on line order between keys that don't look related. `parts/markdownlint-obsidian.md` Implications 4, 7.
- **Settle YAML coercion before writing the type vocabulary.** Docusaurus needed a Joi extension so `tag: 2021` and `date: 2019-01-01` didn't fail "must be a string". `parts/docusaurus-decap.md §JoiFrontMatter` + Implication 7; `frontmatter-linter-config.md §5`.
- **No key-order constraint.** `frontmatter-key-order.md §0` (YAML 1.2.2 §3.2.1/§3.2.2.1 "serialization detail… should not be used"; RFC 8259 §1; JSON Schema Core §4.2.2), §1 hard-nulls (every SSG checked, Prettier by policy, markdownlint, `remark-lint-frontmatter-schema`), §3.1 (Kubernetes folklore refuted three ways, incl. `[executed]` inverted manifest), §3.4 (Terraform: vendor-documented order that `terraform fmt` won't enforce and `tflint` reverted as "personal opinions"). **Already ratified** in `docs/design-adr/0001-no-key-order-constraint.md`.
- **Headline null: nothing surveyed does both halves.** `frontmatter-linter-config.md` §Headline — path-attached *schemas* have vocabulary but no merge; path-attached *settings* merge but have no vocabulary. Closes "copy an existing tool's config wholesale."

### (b) Path &amp; glob precedence / resolution

- **Specificity over globs is not well-defined, and nobody implements it.** `pathrule-precedence.md §3` + §8 final bullet: nginx ranks longest-match over *literal prefixes only*, regexes by file order; Apache depth-sorts `&lt;Directory&gt;` *except regular expressions*; K8s Ingress made specificity definable by restricting the selector language (no glob type); CSS is the only rigorous system and still needs "last declaration in document order wins" plus three accreted escape hatches.
- **Length-as-specificity has failed twice in production.** Traefik (`pathrule-precedence.md §3.4` — a broad regexp outranks an exact host because its string is longer; documented remedy is to abandon the heuristic) and Jekyll, which is worse *because its docs deny it*: docs promise "a more specific path", `has_precedence?` compares `new_path.length`, so `path: "documents"` (9) beats `path: "a/b/c"` (5). `frontmatter-linter-config.md §3.2`; `parts/ssg-family.md` §"Precedence — documented vs implemented".
- **Order, not computed specificity, for a rule list in one file.** `pathrule-precedence.md §1` headline + §7: `.gitattributes`, EditorConfig, ESLint flat config, Prettier, Renovate, tsconfig all independently chose it. *(The "…with per-key merge" half is `[CASCADE]`-stale.)*
- **Match on path segments, not prefixes.** Jekyll's `path_is_subpath?` is a bare `start_with?` with no separator check, so `docs/log` silently captures `docs/logging/`. `frontmatter-linter-config.md §5`; `parts/ssg-family.md` §Jekyll binding + final caution.
- **Selector fields replace; constraint fields merge.** TypeScript carves out exactly `files`/`include`/`exclude` to *overwrite* while merging `compilerOptions`; Renovate declares mergeability per-option (`labels` vs `addLabels`). `pathrule-precedence.md §2.9, §7.2`.
- **Two orthogonal selectors (path + `type`): AND within a rule, rank rules by declaration order.** No surveyed system computes cross-dimension specificity; Renovate is the exact precedent (`matchFileNames` AND `matchPackageNames` AND …, "last one wins" per option). Strategy A (fixed rank per selector class — nginx, Apache, CSS) is the alternative and its cost is an arbitrary memorised ranking. `pathrule-precedence.md §4`.
- **Overlapping scopes are the actual failure class.** Six tools break there (Astro: two collections, one file, two independently validated entries, no diagnostic, zero issues in four years; remark-lint: last match silently wins; markdownlint-cli2: two config families with opposite semantics chosen by filename; VitePress: two divergent merge paths; Hugo: first-writer-wins including earlier-array-element). Eleventy alone avoids the class because directory ancestry is a total order. `frontmatter-linter-config.md §3.4`; `parts/astro-vitepress.md §1.1` + Implication 4.
- **Fail the whole config on a malformed rule; never skip the line.** GitHub: "If any line… contains invalid syntax, that line will be skipped" — in an order-based system a syntax error silently reassigns ownership. `pathrule-precedence.md §7` failure modes.
- **Write the precedence rule into the config file itself.** GitHub ships it as a comment in the CODEOWNERS template; Prettier's total documentation silence on the same question is the counter-example. `pathrule-precedence.md §7.4, §2.7`.
- **Per-directory config files, if ever added: closest-file-wins is well-precedented** — but only for a config file's *literal location*, never a glob's shape. `pathrule-precedence.md §7` final para.

### (c) Severity tiers (error vs warning)

**Effectively unaddressed as a design question.** No file asks it; there is no tier model, no ordering, no statement of which tier a rule may set. Only incidental evidence:

- markdownlint reserves `severity: "error" | "warning"` (and `enabled`) *out of* the rule's parameter namespace before the rule sees `params.config`. `parts/markdownlint-obsidian.md §1.3, §1.4`.
- ESLint's documented *primary intended use* of a later glob-scoped object is "rule severity adjustment (`error`/`warn`/`off`)". `pathrule-precedence.md §2.6, §5`.
- **OKF forces a warning tier by fiat** on five things a consumer MUST NOT reject for (missing optional fields, unknown `type`, unknown keys, broken cross-links, missing `index.md`), plus §6.1 "Consumers MUST tolerate broken links". `okf-conformance.md §3.1, §3.3, §10.7` — "of 16 MUSTs, 6 forbid consumers from rejecting anything."
- Third-party `okft` independently chose "Broken links are warnings, not errors, per the spec's tolerance requirement." `okf-conformance.md §2.2`.

### (d) Violation suppression / per-file opt-out

One firm conclusion, otherwise evidence only.

- **Settled: exemptions need a lock that cannot be re-opened downward.** `parts/markdownlint-obsidian.md` Implication 9 + §1.5. `noInlineConfig` is the right shape but merges as an ordinary top-level key with the child winning, so a nested config should be able to re-open what the root closed — **explicitly flagged as inferred from merge code, not confirmed** (also `frontmatter-linter-config.md §6`).
- markdownlint's full inline vocabulary is documented, including `markdownlint-configure-file`, which lets a *document* set arbitrary rule parameters for itself (`config = {...config, ...parsed}`). `parts/markdownlint-obsidian.md §1.5`.
- **Obsidian Linter is the anti-pattern**: `disabled rules: [all]` in a file's own frontmatter, ranged ignores whose unterminated form silently swallows the rest of the file, and **no config-side switch to forbid any of it** — "governance is unilaterally waivable by the document being governed." `parts/markdownlint-obsidian.md §2.3`.
- **Named exception, not anonymous later rule** — Kyverno's `PolicyException`: separate CRD, "disabled by default", creation "can and should be controlled", "only allow very narrow exceptions to a much broader rule". `pathrule-precedence.md §5, §7.6`.
- **"Say nothing" must be distinguishable from "assert false"** — `.gitattributes`' four states with `!attr`, EditorConfig's `unset`, Eleventy's `override:` prefix. `pathrule-precedence.md §2.4, §2.5, §7.3`; `parts/ssg-family.md` §Eleventy. `[CASCADE]` — under first-match-wins there is nothing inherited to un-say.
- **Not settled:** whether the harness ships any in-file suppression at all. No file proposes a syntax or a policy.

### (e) Report / output format

- **JSON, SARIF, exit codes: zero mentions across all seven files.** Grepped; nothing. Wholly ungrounded.
- **Settled on content, not format:**
  - **Ship the "what governs this path?" resolver.** Two shipped precedents: `git check-attr &lt;path&gt;` (resolves by path string **with no file needed**, and distinguishes `unset` from absent) and VitePress `reportConfigLayers` (~15 dependency-free lines printing the ordered layers that applied). ESLint's own post-mortem names the absence of exactly this as the core failure: *"No one really understood all of the different permutations."* `frontmatter-linter-config.md §3.5` ("the highest-leverage self-teaching feature in the survey"); `pathrule-precedence.md §6, §7.5`; `parts/astro-vitepress.md §2.6` + Implication 4.
  - **A violation must locate the file, name the rule that fired, and preserve the underlying message.** Docusaurus originally shipped *no file path* — reporter: "without context, I don't know where to look at"; maintainer conceded it. Eleventy's `TemplateMap.js` throw is the SSG family's only violation report and is worth copying. `parts/docusaurus-decap.md` §"Error messages"; `parts/ssg-family.md` Implications 1.
  - **Rationale in the emitted message, not in a field nothing reads.** markdownlint's per-rule `Rationale:` paragraph is documentation only — not a field on the rule object, not in `LintError`. Four channels do reach the user: `ruleNames`/`ruleDescription` (per rule), `errorDetail`/`errorContext` (per violation). `parts/markdownlint-obsidian.md §1.4` + Implication 8.

### (f) Non-Node / cross-language portability

**Not addressed at all.** No file surveys it; other languages appear only as the implementation languages of surveyed tools.

- The one transferable observation: **every order-checking tool must bypass its own parser** to see key order — `ruamel.yaml`'s `CommentedMap`, `goccy/go-yaml` under `// Parse the raw YAML to preserve key order`, kustomize's `kyaml`, `sort-package-json` over JSON text. `frontmatter-key-order.md §0(b)`.
- Adjacent lesson: *"absent from the published artifact is not the same as undocumented"* — `defaultRules.md` isn't in the npm tarball, so the README's link is dead for installed consumers. `frontmatter-key-order.md §2.6`.
- The only stated non-Node consumer lives in the **repo's** design-ADR, not the research: `docs/design-adr/0001` defers `position: first` until "a non-Node scanner in a pipeline that genuinely cannot afford a 4 KB read per file" exists.

### (g) Generated files / index generation / progressive disclosure

- **Progressive disclosure: absent entirely.** Zero mentions.
- **Index generation is settled as a fact about OKF, never as harness behaviour.** `okf-conformance.md §6.1`: `index.md` is produced by OKF's first-party `regenerate_indexes()`; generated indexes carry **no frontmatter** (verified: none of 24 committed files begins with `---`); the generator never emits `okf_version`; **no validation happens at any point** and unparseable siblings are silently skipped; §11 guarantees consumers MUST NOT reject for missing `index.md`.
- **The Title-Case `type` convention is purely an artifact of index generation** — `type` is rendered verbatim as an H1 (`lines = [f"# {typ}", ""]`), so `type: adr` would render `# adr`. "A cosmetic consequence in *a generated index the harness does not generate*." Also: falsy `type` falls back to group `"Other"`, and subdirectories get a pseudo-type `"Subdirectories"` that appears in no frontmatter. `okf-conformance.md §5.3`.
- **`log.md` is not skipped by the index generator** (it skips only `index.md`), so a regenerated index lists it as an ordinary entry under whatever `type` it carries. `okf-conformance.md §6.2`.
- Generated files stay out of scope *without a mechanism*, via default-ignore — see (a).

### (h) Editor &amp; Obsidian integration

**No integration design anywhere.** Both appear only as prior art / negative results.

- **Obsidian Linter is a formatter, not a linter** — `Rule.apply` is `string → string`, "there is no violation type in the API to return"; `lintText` returns `string` and the call site writes straight to disk; the documented pipeline reserves reporting for *crashes*; diff-before-write is open FR #1515. No path scoping (two global ignore lists; folder matching is a `startsWith` prefix test, not a glob). Config is UI state in `.obsidian/plugins/obsidian-linter/data.json`, which its own docs describe as a bug-report attachment. Four years of open requests for path-scoped settings (#1213, #555, #1147, #849). YAML values are "parsed via regex instead of a library at this time". `parts/markdownlint-obsidian.md §2`.
- **Editor schema binding:** `vscode-yaml` publishes its precedence (modeline &gt; in-file `$schema` &gt; `yaml.schemas` &gt; SchemaStore), single-winner throughout. **SchemaStore has no frontmatter story** — of 1425 entries / 1333 `fileMatch` patterns, **zero** end in `.md`/`.mdx`/`.markdown`; `schemastore.org/frontmatter.json` 404s; and `$schema`-in-frontmatter *conflicts with the spec*, where `$schema` names a **dialect** and "MUST be a URI", not a file path. `frontmatter-linter-config.md §1`.
- Astro emits per-collection JSON Schema to `.astro/collections/` **solely as an editor hint**, lossy by construction. `parts/astro-vitepress.md §1.0, §1.5`.
- A CMS round-trip will silently rewrite key order (Decap sorts on `toFile()`, plain `toJSON()` on `fromFile()`, never validated). `frontmatter-key-order.md §1.3` final caveat.

---

## 2. Left open — flagged as unresolved by the files themselves

- **`pathrule-precedence.md §8`** — Renovate's *published* `packageRules` prose (precedence quoted from a maintainer discussion, not the manual; "order in ascending importance" not verified verbatim); whether Prettier's `overrides` conflict behaviour is *intended* rather than incidental; Selectors L4 §15 verbatim (`:is()`/`:not()`/`:has()` specificity); any CSS-WG statement acknowledging "specificity wars" (the motivational reading is inference); any cross-policy precedence rule in OPA Gatekeeper or Kyverno; **any widely-adopted system ranking overlapping globs by computed specificity — none found.**
- **`frontmatter-linter-config.md §6`** — whether a Hugo descendant can *clear* an inherited cascaded value (undocumented, untested upstream); Eleventy's deeper-schema-replaces-shallower is **source-derived only**; markdownlint-cli2's `noInlineConfig` re-openability is **inferred, not confirmed**.
- **`frontmatter-linter-config.md §7` ("Opens")** — whether `required` defaults `true` (Decap) or `false`; what `null` and arrays do on merge; segment-aware glob vs prefix matcher; whether `combine:` is explicit per rule; whether the §3.5 explainer ships in v1. *Of these, only `required`-default, segment-vs-prefix and the explainer survive the cascade removal.*
- **`okf-conformance.md §9`** — whether `log.md` may carry frontmatter (**genuinely unresolved upstream**, #286, no maintainer reply; §8's no-frontmatter rule is written only for `index.md`, §9 says nothing, and the reference bundle ships `type: Log` frontmatter; only 1 of 4 bundles has a `log.md` at all, so examples don't settle it); any maintainer position on v0.3 scope or timing; whether the knowledge-catalog backlog migrates; whether `okf_version` will ever be read by anything.
- **`okf-conformance.md §5.6, §10.4`** — `type` casing is house policy either way; enum-vs-case-scheme is left to the repo, with the hard constraint that *"the ADR must not claim OKF requires it."*
- **`frontmatter-key-order.md §6`** (7 items) — `cargo-sort`'s default path not executed (README contradicts source); kustomize `cfg cat --format` source-verified only; no rationale located for kustomize's intra-group order; `kubernetes/website` not searched exhaustively; **"do not cite a yamllint maintainer refusal of custom orders — none exists; nobody asked"**; Debian `deb822` checked at Policy level only; `eslint-plugin-json-files` unpinned.
- **`parts/astro-vitepress.md §1.1, §2.2`** — Astro's overlapping-collection-glob behaviour is read off source, undocumented, with *zero* upstream discussion (GitHub search returns 0); whether VitePress documents or guarantees that throwing from `transformPageData` fails a build **could not be established**.
- **`parts/ssg-family.md`** — Hugo's explicit-YAML-null-suppresses-cascade: **unverified** (no doc, no test in 600 lines of `cascade_test.go`); Jekyll's nil-in-narrower-scope: **unverified**; Eleventy with two schemas at different cascade levels: no doc statement and no test, source-derived only.
- **`parts/docusaurus-decap.md`** — no primary source on what Decap does when two collections' folders overlap **by path** rather than by `filter`; the schema requires only that collection *names* be unique.

---

## 3. Stale premises

### 3.1 The known one, and every downstream repeat of it

`frontmatter-linter-config.md:13-16` — "this harness cannot use Zod, Joi or AJV. Rule files may import only `node:path`, `node:url`, `node:util`, `node:crypto`, enforced by a static scanner. Config must be data, parsed via `ctx.readYAML()`." The same archgate premise recurs at:

- `frontmatter-linter-config.md §3.6:184` — "the harness cannot use libraries in `.rules.ts` anyway"
- `frontmatter-linter-config.md §5:245` and `parts/docusaurus-decap.md` Implication 7:407 — "A hand-rolled checker over `ctx.readYAML()` hits the same wall"
- `parts/astro-vitepress.md` Implication 7:346 — "no third-party libraries in `.rules.ts`, so 'off our shoulders' is unavailable"
- `parts/ssg-family.md:415` — Eleventy's zod schema is "executable code, which the harness has ruled out"; :346 counts "a JS runtime and a third-party dep" as a cost
- `frontmatter-key-order.md §0(b):117-119` — *"**This harness already does the same thing, out of necessity**: `ctx.readYAML()` returns `frontmatter: Record&lt;string, YamlValue&gt; | null` (`.archgate/rules.d.ts`)… so `GEN-001-adr.rules.ts` regexes the raw block instead"* — the mechanism, the type and the file are all archgate's

Note the config-is-data **conclusion** is independently supported (Astro's lossy round-trip, Docusaurus's dead end, Decap's positive enumeration) and survives; only the *reason given* is dead.

### 3.2 The unnamed big one — the cascade is gone

`CONTEXT.md` ("Rule"): first matching Rule is the complete constraint set; **nothing merges and nothing is inherited.** Now-false or moot:

- `pathrule-precedence.md §7` recommendation in full ("all matching rules merge in declaration order, later wins, merge granularity per leaf key") and design points 1–3; §5 "relaxation should be ordinary"; §6's `deep_merge(result, rule.constraints)` sketch; §7's failure mode "someone adds `**/*.md` at the bottom and silently relaxes every earlier rule".
- `frontmatter-linter-config.md §3.1` — *"The harness is in the third family. This makes preference 5's say-nothing token a **structural requirement**"*; §5 "Specify merge depth explicitly" and "Steal `combine: merge|replace` as a **required** key" and "the harness's `docs/` → `docs/logs/` per-key relax is one level deeper than it merges"; §3.4 "The harness permits arbitrary overlapping selectors, so it inherits this hazard **by design**"; §7 "Opens" (`null`, arrays, `combine:`).
- `parts/markdownlint-obsidian.md` Implications 2, 3, 5 — cli2's leafward-relaxing cascade cited as "a deployed instance of **the model this harness settled on**"; "the merge must recurse to `description.required`"; `combine:` required.
- `parts/astro-vitepress.md` Implications 1, 2, 3 — "copy `stackView`'s shape"; "decide once, in the ADR, what `null` means and what arrays do"; "an ordered cascade over rule *objects* relaxes trivially".
- `parts/ssg-family.md` Implications 2, 3 + Implications table — "The harness's ordered cascade merging at the leaf, deepest last, is the Jekyll `all()` shape".
- `parts/docusaurus-decap.md` Implications 5, 6 — "If the harness's cascade works, it solves what two mature tools both punt on."

**What survives the change:** order-not-computed-specificity; segment-not-prefix matching; the intent/message findings; default-ignore; report-never-rewrite. And **first-match-wins is itself precedented** — markdownlint-cli2's `overrides[].filter` ("only the first matching `Override` is used", `parts/markdownlint-obsidian.md §1.2`), nginx's regex-first-match, Hugo's first-writer-wins (`parts/ssg-family.md`). The "what governs this path?" explainer gets *more* important, not less: a single winner means every losing rule is silent.

### 3.3 Floor / `types:` ceiling

- `CONTEXT.md` marks **Floor retired**; all five families (`type` presence, `type` membership, `generated.by`, `sources[].resource`, Actor form, timestamp format) are ordinary per-Rule Constraints; "treat every such mention as history."
- `pathrule-precedence.md §5` final para and **§7.6** are the *origin* of the Floor idea — "a small monotone floor alongside the cascade, not a rule inside it", "evaluated outside the cascade" (IAM/OPA/Kyverno lineage). Read as a proposal tried and removed, not a live recommendation.
- No file uses the phrase "`types:` ceiling" — that premise is implicit. The nearest artefact is `okf-conformance.md §5.6, §10.4`'s "closed allowlist/enum of the handful of types this repo actually uses, labelled explicitly as house policy", which the current design realises as per-Rule `allowed` records with **no repo-wide declaration** (`CONTEXT.md`, "Type vocabulary": "implicit: the union of those records across the config").

### 3.4 Other archgate-as-engine premises

- **`okf-conformance.md §1` summary row + §5.6** — *"Should the harness ship a `checkCase` gate on `type`? **No** — archgate's schemes cannot even express the OKF convention"*, quoting `CaseScheme` from `.archgate/rules.d.ts` and "Archgate 0.55 exposes `ctx.checkCase(value, scheme)`". The **conclusion** (labelled house-policy allowlist, not a case scheme — an enum also catches `adrs`/`ADR`) survives; the **reason** is a dead capability limit.
- **`frontmatter-key-order.md` is archgate-premised end to end** — its research question is whether `files` joins `GEN-001-adr`'s enforced prefix; §5's grounding facts are archgate's ADR schema `{id,title,domain,rules,files?,respectGitignore?}`, `.archgate/adrs/` containing exactly one ADR, `files` landing in commit `5812f8f`, and Claude Code's `paths`. `docs/design-adr/0001` records that question as **"answered upstream and shipped"**. Its §5 "Licensed / Not licensed" apparatus concerns a rule this repo does not have. Retained value: §0 (the normative floor), §1 (the hard nulls), §3.4 (Terraform), §4 (change cost) — exactly what `0001` cites.

### 3.5 Further assertions now false

- **"Generic, never type-aware" vocabulary rule.** `parts/astro-vitepress.md` Implication 6 ("Given the harness's 'generic, never type-aware' vocabulary rule **already forbids** that class of check") and `parts/docusaurus-decap.md` Implication 4 (generic keys "never type-aware names" as the property to copy). `CONTEXT.md` ("Constraint") now says the opposite: **"Constraints are shape-specific by construction: `minLength` names strings, `minItems` names lists."**
- **Repo and product coordinates.** `frontmatter-linter-config.md` and `frontmatter-key-order.md` link `hancrafted/okf-frontmatter-harness` issues #3/#7/#9/#14 throughout. Repo is now `hancrafted/markdown-harness` (`AGENTS.md`), the product is a CLI + MCP steering API of which `frontmatter-harness` is *one Module*, and **OKF ships as a Preset, not as behaviour** (`CONTEXT.md`; `docs/okf/README.md` "What reads it: **Nothing, mechanically**").
- **`okf-conformance.md §10.2 and §10.3 are already done**, so they read as open when they are closed.** The spec is vendored byte-identical at `docs/okf/SPEC-v0.2.md` with the same `sha256 26aa5da0…` recorded, and the canonical URL is updated. §10.2's framing is also superseded in emphasis: `docs/okf/README.md` states the sha **"is provenance, not a gate"** because git already content-addresses the file, and "**this file is the pin**", not the hash.
- **"OKF's one mandatory field is `type`" as a harness property.** `frontmatter-linter-config.md §5` final bullet ("Interop landmine: Hugo reserves `type`… Any Hugo-based repo adopting this harness **collides on the single field OKF requires**") assumes unconditional `type` enforcement. Post-Floor, `type` presence is an ordinary per-Rule Constraint and unmatched files are ungoverned, so the collision is opt-in rather than structural. *(The Hugo fact — "you cannot create a custom field named `type`", use `params` — is still true and still worth the README.)*
- **`type: adr` as the repo's record discriminator.** `okf-conformance.md §4, §10.5` keeps `adr`/`skill`/`log`. The repo now runs **two** record kinds discriminated by value — `adr` and `design-adr` — which the file predates.
- **Live-tracker snapshots, ~2026-08-22.** `okf-conformance.md §0, §8` — "no migration has occurred", "canonical repo has **0 issues**", "no maintainer has replied to **any** of them", "186 open issues". Re-check before quoting; the file's own §0 warns v0.2 mutates in place under a fixed label.

---

## 4. Prior art worth naming in a README

| Tool | Why it doesn't fit (one line) | Cite |
|---|---|---|
| **`remark-lint-frontmatter-schema`** | The closest existing tool, and it **silently drops rules**: schema→globs with last-match-wins and no merge (`index.ts` L249-268 overwrites, never breaks — undocumented, untested), raw AJV errors so a schema's `description` never reaches the diagnostic, and being JSON-Schema-based it cannot relax an inherited `required` at all. | `frontmatter-linter-config.md §1, §2` |
| **JSON Schema + AJV directly** | Subtractive by construction — Core §10.2's normative "MUST NOT impact sibling subschemas" makes relaxation structurally impossible; no `unevaluatedRequired`/`notRequired`/`optional`; and it cannot express key order either. *"How do I model an inheritance hierarchy in JSON Schema? You don't."* | `frontmatter-linter-config.md §1`; `frontmatter-key-order.md §0(c), §1.3` |
| **markdownlint / markdownlint-cli2** | Does not validate frontmatter *at all* — the block is stripped before parsing and handed to rules as an opaque `string[]`; `markdownlint@0.41.1` ships no YAML parser. All frontmatter "understanding" is `.some(line =&gt; /^\s*title\s*[:=]/.test(line))`, used only as a suppression gate. Field-level governance means re-implementing this harness inside its custom-rule API. | `parts/markdownlint-obsidian.md §1.1` |
| **Obsidian Linter** | Closest *name*, opposite tool: a **formatter** that rewrites in place with no violation type in its API, parses YAML "via regex instead of a library", has zero path scoping (4 years of open FRs), and keeps config as UI state in a vault-private `data.json`. | `parts/markdownlint-obsidian.md §2` |
| **Astro content collections** | Code config; flat, one-level-deep collection namespace (nesting ruled out in RFC 0027); relaxation only via *undocumented* Zod `.partial()` bound to a collection **name**, not a path; unknown keys silently stripped (`z.object`, not `strictObject`); one file in two collections validates twice with no diagnostic; errors are Zod's locale — `Too small: expected string to have &gt;=40 characters`, never the reason. | `parts/astro-vitepress.md §1` |
| **Docusaurus** | The anti-pattern, citable as one: hardcoded Joi per content plugin, `allowUnknown: true` forced on every call, **no frontmatter hook in plugin options at all**, granularity stops at the plugin root, not one top-level key is `.required()`, and the tracker contains no request for a configurable schema — only "add my field to your enum". | `parts/docusaurus-decap.md` §Docusaurus |
| **Decap CMS** | Best config *shape* in the survey (positive closed labelled enumeration, `required` defaults **true**, `pattern: [regex, message]` as one data pair) — but validation is **browser-only React component code** with no lint/validate CLI anywhere in a 40-package monorepo, so a committed file never passes through it; and there is no `extends`/inherit/cascade key anywhere in its schema. | `parts/docusaurus-decap.md` §Decap |
| **Hugo `cascade` / Jekyll `defaults` / Eleventy dir data** | Default-*setters*, not constraint systems: Hugo and Jekyll cannot express a single negative assertion; Eleventy can only via `eleventyDataSchema`, which is arbitrary JS whose deeper schema *replaces* rather than composes. Plus Hugo **reserves `type`** outright and Jekyll's precedence is the byte length of the scope string. | `parts/ssg-family.md`; `frontmatter-linter-config.md §5` |
| **VitePress** | No frontmatter validation of any kind (`frontmatter: Record&lt;string, any&gt;`; misspelled keys silently ignored). Name it only to credit `reportConfigLayers` — the shipped precedent for the steering answer. | `parts/astro-vitepress.md §2` |
| **`okft` / `okf-skills`** (3rd-party OKF validators) | Neither is first-party or maintainer-endorsed; `okft` targets **OKF v0.1** and predates the v0.2 migration. *"The harness must not treat either as the interface."* | `okf-conformance.md §2.2` |
| **OKF's own reference agent** | The only first-party conformance artifact is `OKFDocument.validate()` — **4 lines**, checking that `type` is present and truthy, called only on the *write* path of the generating agent; readers `except Exception: return None`. No `okf validate`, no `okf lint`, no JSON Schema, no conformance suite in either repo. | `okf-conformance.md §2, §2.1` |
| **`vscode-yaml` / SchemaStore** | No frontmatter story exists: 0 of 1333 `fileMatch` patterns end in `.md`/`.mdx`/`.markdown`, `schemastore.org/frontmatter.json` 404s, and `$schema`-in-frontmatter contradicts the spec ($schema names a *dialect* and "MUST be a URI"). | `frontmatter-linter-config.md §1` |

**Also worth a README line as *mechanism* precedent rather than competitor:** `git check-attr &lt;path&gt;` — the steering query already exists for `.gitattributes`, resolves by path string with no file needed, and distinguishes `unset` from absent (`frontmatter-linter-config.md §3.5`).</result>
<usage><subagent_tokens>156060</subagent_tokens><tool_uses>21</tool_uses><duration_ms>366935</duration_ms></usage>
</task-notification>


---

## Assistant  ·  2026-08-26T13:31:32.086Z

Second exploration is back. It contains **one finding that contradicts my own Q3 recommendation** and one that undercuts your headline automation idea, so I'm amending rather than waiting silently. Round 1 is still 6 questions.

**⚠️ Q3 — my recommendation collides with a settled instruction.** "**Report, never rewrite**" appears twice in the harvested research as an explicit ADR instruction (`frontmatter-linter-config.md §5`; `parts/markdownlint-obsidian.md §2.1` + Implication 10). It was concluded *against* Obsidian Linter, whose `Rule.apply` is `string → string` with "no violation type in the API to return" — a formatter wearing a linter's name, writing straight to disk.

I still think derived-only writes are right, but I was wrong to present (b) as uncontested. The honest shape of Q3 is now:

- **(a)** Read-only, no carve-out. Honours the instruction literally. The index is generated by *your agent*, from a work order.
- **(b′)** Read-only for **governed** files — the instruction's actual target — with a narrow carve-out for files the Harness **wholly owns and regenerates from scratch**. The carve-out needs a stated boundary, or it becomes `--fix` by increments.
- **(c)** `--fix` anywhere. Rejected by the research.

➡️ Still **(b′)**, but the boundary has to be written down as a rule, not a habit: *the Harness may write a file only if it authored 100% of that file's bytes and can reproduce them byte-identically from data it read.* One partial-ownership exception — a marked region inside a hand-authored file — and you're Obsidian Linter with extra steps.

**⚠️ Your index automation may be reinventing an OKF function.** `okf-conformance.md §6.1`: OKF ships a first-party `regenerate_indexes()`; generated indexes carry **no frontmatter** (verified across 24 committed files); **no validation happens at any point** and unparseable siblings are silently skipped; and §11 guarantees consumers MUST NOT reject a directory for a missing `index.md`. Also — `type` is rendered verbatim as an H1 (`# adr`), which is the *only* reason OKF's examples use Title-Case types. So: does the Harness generate indexes, *govern* the output of OKF's generator, or is your progressive-disclosure surface a different artifact from OKF's index? Notably, **"progressive disclosure" appears zero times across all seven research files** — this is unbroken ground, and your `fixtures/valid-test-config.yaml` already declares `fileName: index.md → frontmatter: forbidden`, which is consistent with OKF but means a generated index is a body-only write.

**Q1 gains its strongest supporting fact.** The steering query is not an invention — `git check-attr <path>` is the shipped precedent: it resolves by path string **with no file on disk**, and distinguishes "unset" from "absent". VitePress's `reportConfigLayers` is a second, ~15 dependency-free lines. And ESLint's own post-mortem names the *absence* of this as the core failure: *"No one really understood all of the different permutations."* The research calls it "the highest-leverage self-teaching feature in the survey." Under first-match-wins this matters **more**, not less: a single winner means every losing Rule is silent.

Also for Q1: the "why not just use X?" section I recommended now has a fully-cited table — 12 tools, including `remark-lint-frontmatter-schema` (closest existing tool, and it *silently drops rules*: last-match-wins with no merge, undocumented and untested) and the fact that **OKF's own only first-party conformance artifact is a 4-line `validate()`** that checks `type` is truthy, on the write path, in the generating agent.

**Q5 is the one question with zero research behind it.** Cross-language portability: *"not addressed at all"* — other languages appear only as the implementation languages of surveyed tools. Everything else in this round has prior art; this one you'd be answering from first principles, which is exactly why the default (drift into Node-shaped assumptions) is the risk.

One queued question is now better grounded than I said: severity tiers aren't unaddressed, they're **contested**. Preference 5 says all rules run at `error`. But OKF forces a warning tier by fiat — *"of 16 MUSTs, 6 forbid consumers from rejecting anything"* (`okf-conformance.md §3.3, §10.7`), including broken cross-links and missing `index.md`. So the OKF **Preset** would have to enforce as errors several things the spec forbids rejecting. Resolvable — OKF constrains *consumers*, not authoring gates — but it needs saying out loud, not assuming.

Holding for your Round 1 answers: **Q1** promise · **Q2** audience + git · **Q3** writes (revised above) · **Q4** who runs the model · **Q5** portable artifact · **Q6** dependency budget.


---

## Human  ·  2026-08-26T13:41:21.257Z

Q1: C, just `Trust`. I think trust is a good north star to build and have trust in your own system, Even if you don't understand every part of it and even over longer time. So a vision here is that using this product gives you a minimum floor that you can always trust.  
Q2: My thoughts were the preferred is a git back repository on GitHub or any other git supported repositories. Failing that I would set a local git as the floor for now. If a skill or a setup script can provide that Git is installed and usable on the local Windows machine, then that should be possible. As I said, I'm currently assuming that you have at least administrative rights to your own laptop. 
Q3: This is a good question as another base assumption I'm having is it's not a standalone harness but rather the user is using one of the available harnesses so like cloud code, codecs, maybe we can even make a deep seek harness which is open source right as a
 possible reference. And those are the primary harnesses that use the agents. And this is just something on top or a plug and play, something like this. I'm still considering this a harness, just not something which is defining all the tools and limitations and permissions. We don't need to reinvent that. Hence a requirement would be that this harness can be has a plug and play or integrability to the other harnesses. If what I'm describing is not a harness here, then we probably need to settle the term as early as possible so we know what we're talking about. 
Q4: I think this is part of question three. So I like where B is heading. So yeah, probably this harness is able to schedule and call like Claude-P or something like this. But that is up for discussion. As I said, I want to keep it lightweight and if If we can set it up in a way that other harnesses run the model, run the code, and we are just providing instructions and governance, the better. But this is challengeable and I would like to know your opinion. 
Q5: Yes, you got it right. So this tool should be able to run in different engineering environments. But if we can architect it in a way that right now we start with Node and NPM only to make the initial proof of concept and minimal viable product more easy, while we keep this as a North Star, I'm also fine. 
Q6: This is something we need to work out together. I don't have a definitive answer right now. My assumption is the fewer dependencies we have, the easier it is to be adopted, even on enterprise level. But obviously the big SDKs for AI development to create MCP servers, for example, we can include those. I mean, using TypeScript, obviously, I have some TypeScript dependencies already. But this is an open discussion, I would say. In general, I would say we only include libraries with larger than 1000 stars and maintained within the last six months. That should be a good filter to have considering which dependency we want to include. And generally, even the YAML dependency was installed prematurely, I think. So really, I don't want to, so if something we can implement ourselves within 100 lines of code, that might be another good metric, but this is something I like your opinions on as well. 

I have only read the initial 6 question, without the update from backgorund checks.


---

## Assistant  ·  2026-08-26T13:43:35.238Z

Six answers, and one of them opens a term collision I have to flag before I can write another sentence of prose. Let me get the dependency facts first — that's my job, not yours.


---

## Assistant  ·  2026-08-26T13:43:36.544Z

`[tool] Bash — Check dependency trees of candidate runtime deps`


---

## Assistant  ·  2026-08-26T13:43:38.666Z

`[tool] Bash — Measure real install footprint of candidate deps`


---

## Assistant  ·  2026-08-26T13:43:48.945Z

The picomatch measurement is contaminated — `npm view` shows it has zero dependencies. Re-measuring cleanly:


---

## Assistant  ·  2026-08-26T13:43:52.226Z

`[tool] Bash — Re-measure picomatch and yaml footprints cleanly`


---

## Assistant  ·  2026-08-26T13:44:48.266Z

Clean numbers now. One more fact worth having before I ask about dependencies — Node may already ship glob matching:


---

## Assistant  ·  2026-08-26T13:44:51.067Z

`[tool] Bash — Check Node built-in glob support and semantics`


---

## Assistant  ·  2026-08-26T13:46:20.002Z

Node 26.5 ships `path.matchesGlob` **and** it is segment-aware: `docs/logging/x.md` vs `docs/log/**` → `false`. That's precisely the Jekyll bug the research flagged (`path_is_subpath?` is a bare `start_with?`, so `docs/log` silently captures `docs/logging/`). Relevant below.

## Settled in Round 1

| | |
|---|---|
| **Q1** | **Trust** is the promise. Not conformance, not steering — those are mechanism. |
| **Q2** | Git-backed repo is the target (GitHub or any host); **local git is the minimum**; a setup wizard may provision git on Windows; admin rights on the user's own laptop assumed. |
| **Q4** | Direction (b) — work orders. Open to the Harness invoking `claude -p`. Wants my opinion. → Q9. |
| **Q5** | Node + npm for the MVP; cross-language as north star, contract declared now. |
| **Q6** | Open. Your proposed screens: >1000 stars, maintained ≤6 months, "if it fits in 100 lines, write it". → Q10. |
| **Q3** | **Not answered** — you pivoted to integration, which turned out to be the more important question. Carried; it now depends on Q8. |

Your Q2 answer is *stronger* than what I recommended, and I'll take it, because it buys something you may not have connected: **making git a requirement is what makes the anti-loosening check possible at all.** Issue #32 called that "the honest mechanism." See Q8. The synthesis I'd propose: git is required to *adopt* and required for the loosening guarantee, but `check` and `steer` themselves make no git call — so the fast offline loop stays fast and offline.

## ⚠️ You used a retired term three times

You said "a minimum **floor** you can always trust", and "local git as the **floor**". `CONTEXT.md` defines **Floor** as *retired*, with an explicit `Avoid` line — it named the removed unconditional per-file requirements, and the glossary says "treat every such mention as history."

This is not pedantry: any agent that reads `CONTEXT.md` and then reads a README promising "a minimum floor you can always trust" will resolve *floor* to the retired concept and conclude the Floor is back. Given the whole point of Q8 is what the guarantee *is*, this word has to be different.

➡️ **Use "Guarantee" for the trust baseline, and "minimum requirement" for things like git.** Keep **Floor** retired, untouched.

## From the background checks you haven't read — two items bearing on this round

**"Report, never rewrite"** appears twice in the harvested research as an explicit ADR instruction, concluded against Obsidian Linter (a formatter whose API has no violation type). It doesn't settle Q3, but it means a write capability needs an argument, not an assumption.

**OKF already generates indexes.** `okf-conformance.md §6.1`: a first-party `regenerate_indexes()` exists; generated indexes carry **no frontmatter**; **no validation happens at any point**; unparseable siblings are silently skipped; and consumers MUST NOT reject a directory for a missing `index.md`. Your headline automation may be reinventing it — or governing it. Q8 decides which.

---

❓ **Q7** — **Settle "harness", because it currently means three things in one conversation**: You asked for this early, and you're right to. Right now: (1) `CONTEXT.md`'s **Harness** = "the package an adopter installs" — this product; (2) your new usage = Claude Code / Codex / Antigravity — the thing that owns the model, the tools, the permissions and the session; (3) `typescript-ai-harness` = a scaffolding installer. Three referents, one word, and the product's own name is built from it.

For thing (2), the options are: **(a) Agent host** · **(b) Agentic CLI** (your own phrase) · **(c) keep "harness" for both and always qualify.**

➡️ **(a) as the glossary term, (b) as the plain-English gloss for non-technical readers, and an explicit `Avoid: harness` on the entry.** (c) fails the only test that matters — an agent reading a bare "the harness" must resolve it correctly with no context.

**And keep `markdown-harness` as the product name.** A wiring harness constrains and connects without being the thing it carries; that's exactly this relationship, and #32 settled repo/npm/bin already. But say it out loud: **this week is the cheapest a rename will ever be** — nothing is published, one repo, and maybe six files reference the name. If the workshop audience makes you wince at the word, now is the moment, not October.

The definition I'd write, which also makes "plug and play" concrete: *markdown-harness is a governed substrate and a steering source that any Agent host plugs into. It has no model, no tools, and no permissions of its own.* Three integration surfaces, in dependency order: **CLI** (`mh check`, `mh steer`) → **MCP server** (steering at write time) → **generated agent-facing docs**. Push back if that's the wrong set or the wrong order.

---

❓ **Q8** — **What does "trust" cash out to? The Guarantee, stated as something checkable**: "Trust even if you don't understand every part, even over a long time" is a good north star and not yet a spec. A guarantee has to be cashable — a thing that is either true of your repo or reported false. Four candidates, and they are four different products:

**(A) Conformance** — every Governed file satisfies its winning Rule, or the check is red. *Trust = the structure is intact.* But a perfectly conformant document can be stale nonsense.
**(B) Provenance** — every Governed file states what produced it, from which sources, and when it expires. *Trust = you can tell where a claim came from without reading it.* This is literally "trust without understanding every part", and it is what OKF's vocabulary exists for (`generated.by`, `sources[]`, `verified[]`, `stale_after`).
**(C) Reviewability** — the set of documents you must personally review is bounded, enumerable, and current. *Trust = you can see the whole surface.* This is the anti-explosion answer and your index idea.
**(D) Non-degradation** — nobody, human or agent, can quietly loosen the rules. *Trust = the governance itself cannot rot.*

➡️ **(B) + (D) are the Guarantee. (A) is the mechanism that delivers them. (C) is the first thing built on top, not part of the promise.**

Here is why (D) is not optional, and it's your own story: your earlier vaults were governed. Something maintained them. What failed wasn't that files became malformed — it's that governance drifted while the checks stayed green, so green stopped meaning anything. **Without (D), (A) and (B) are self-certifying.** Every agent that finds a Rule inconvenient relaxes it, every subsequent run passes, and you are exactly back where you started — trusting a system whose standards you can no longer see.

**And (D) is the load-bearing decision you asked me to surface, because it constrains the config language and it is cheap only right now.** Detecting a loosening means diffing the config against its git base and mechanically answering *"is the new value looser?"* — which requires **every constraint key to carry a declared direction**:

| change | verdict |
|---|---|
| `maxLength: 200 → 500`, `minLength: 3 → 1`, `minItems: 1 → 0` | looser |
| `presence: required → optional` | looser |
| `allowed: [a,b] → [a,b,c]` | looser |
| `unknownKeys: forbidden → allowed` | looser |
| dropping a `fields` entry · deleting a Rule · adding `excludeFiles` · narrowing a `path` glob | looser |
| **`pattern` changed** | **undecidable — regex containment isn't practically computable. Must be flagged unconditionally.** |

So 11 of your ~12 keys have a derivable direction and one never will. That yields a rule for `ARCH-002`: **no constraint key may be added whose loosening direction is undecidable, unless that's a knowing exception like `pattern`.** It also retroactively justifies a choice you already made — a shape-agnostic `min` would have had *ambiguous* direction, so the shape-specific `minLength`/`minItems` split was load-bearing for a reason nobody wrote down yet. Add keys for six months without recording direction and you either retrofit by re-reading every key, or ship a loosening check that silently misses half of them — which is **worse than none**, because it certifies.

---

❓ **Q9** — **Work order as the contract, or does the Harness drive the Agent host?** You asked my opinion, so here it is plainly.

**(a)** The work order is the only contract. The Harness emits machine-readable work; something else executes. No subprocess, ever.
**(b)** Work order is the contract; an optional, thin, deletable adapter can invoke `claude -p` / `codex exec` for convenience.
**(c)** The Harness drives the host. `mh refresh` shells out and does the work; the work order is internal plumbing.

➡️ **(b), and the reasoning comes straight out of Q1.** An opaque agent run that touches forty files is *the exact event* that destroys trust — and under (c) that run is the product. Under (b) the flow is **emit → inspect → execute → deterministic re-check → report what actually changed**, and every step but one is deterministic. The work order is inspectable before and diffable after; that's what makes delegating judgment survivable.

Two consequences worth accepting explicitly. **The re-check is not optional** — `claude -p` is non-deterministic and can partially fail, so without a deterministic verification pass afterwards you've built a system that reports success based on nothing. And **each host becomes a ~30-line adapter behind one interface**, which is what makes your DeepSeek idea a contribution rather than a fork. Under (c), every host is a rewrite and Anthropic changing a flag breaks your product.

The `intent` field you already made mandatory is exactly the payload a work order needs. That's what it was for; it just didn't know it yet.

---

❓ **Q10** — **The dependency policy, with three live cases and measured numbers**: Your screens — >1000 stars, maintained ≤6 months — are a **reputation** filter, not a **risk** filter. They say nothing about transitive count, install scripts, license, or how much of the package you actually use. Measured, just now:

| candidate | packages installed | size | direct deps | licence |
|---|---|---|---|---|
| `yaml@2.9.0` | **1** | 1.3 MB | **0** | ISC |
| `picomatch@4.0.7` | **1** | 120 KB | **0** | MIT |
| `minimatch@10.2.6` | 2 | — | 1 → `brace-expansion` | BlueOak |
| `@modelcontextprotocol/sdk@1.30.0` | **90** | **25 MB** | **17** | MIT |

➡️ **Four positions, and I'd sharpen your 100-line rule rather than adopt it as written:**

**Invert it: if you can write it in 100 lines *and a bug would be loud*, write it. If a bug would be silent, buy the library.** Loudness is the variable that matters for a trust tool — a crash is a bug report, a silent misparse is a false green.

**1. `yaml` stays, and it's the exception that proves the rule.** One package, zero deps, ISC, no install scripts. A hand-rolled YAML reader is the highest-risk code you could possibly write here: YAML 1.1 vs 1.2 booleans (`yes`/`no`/`on`/`off`), `tag: 2021` coercing to a number, `date: 2019-01-01` coercing to a Date — Docusaurus needed a whole Joi extension for exactly that — plus anchors, aliases and multi-line scalars. And the research's named anti-pattern, Obsidian Linter, parses "YAML values via regex instead of a library at this time." Silent misparse is the worst bug available to you. Not premature; the best-justified dependency in the tree.

**2. MCP: hand-roll, and the numbers make the argument.** 90 packages and 25 MB for a local stdio server, pulling express, hono, jose, ajv, zod, cors and express-rate-limit — essentially all of it for HTTP transports and OAuth that a stdio server never touches. It would also put a 90-package graph between you and the single-binary north star. stdio MCP is newline-delimited JSON-RPC 2.0.

**3. Globs: probably zero dependencies — Node already has this.** `path.matchesGlob` exists on Node 26.5 with no experimental warning, and it got the segment-boundary case right in the test above. If you're on Node 24+ anyway, that's the whole feature for free, and `picomatch` (120 KB, zero deps) is the fallback if its semantics prove too thin. **But this is where Q5 bites**: if the config language is the portable spec, glob semantics are part of your public contract, so **whatever you use, the semantics get written into the spec and pinned by the fixture corpus** — otherwise a Go port has nothing to hit and you've quietly made a Node implementation detail into your product's behaviour.

**4. State the budget as a number, in the README.** *Runtime dependencies: 1 (`yaml`). Adding one requires an ADR.* A number invites the question "why is this here?" the way "as few as possible" never does. And note — this is the one place a `rules: true` ADR would genuinely earn its keep, since dependency count is trivially checkable, whereas #32 has all new ADRs at `rules: false`. Flagging, not deciding.

---

Still queued: the write boundary (Q3, now downstream of Q8) · the term and ownership model for generated files · severity tiers — **contested**, not open: preference 5 says all rules run at `error`, but OKF forbids consumers rejecting for 6 of its 16 MUSTs · suppression · report format · the config filename · the October cut line · CI/CD gates · and which documents this session writes.


---

## Human  ·  2026-08-26T14:04:11.920Z

Q7: I would prefer a more like a negative space so whenever I or you talk about hardness without any context the agent is required to clarify which hardness means and I need to get used to talk specifically just markdown hardness type gr harness. because cloud code, codecs and anti-gravity for example, they are all referred to as harnesses. 
Q8: This is a very good question. This system will never be able to generate full trust. That is just impossible. The mechanism it deploys should guarantee, to use a new word, a specific set of criteria which helps the human. number one obviously the configuration which fixes certain criterias like a type so allowed type for example or min or max length those are obviously a very low and low level of guarantee but they are still there. Then what we should strive for is I would call it a signal. So when a document becomes stale or if it hasn't been updated for quite some time and if it is suddenly being consumed because some agent
 two or three months later discovers it, the document should be able to signal, hey, cool that you found me, but I am three months old, so maybe you should verify against staleness before you take what I've written as it stands. Even if my wordings in my body says these are decisions,
 like iron clad decisions made. This is exactly the reason why I chose Google OKF, especially with the 0.2 version, since it comes with properties that enables the check of this kind of behavior. to your options so conformance is I would say the very basic guarantee if I understand correctly but it does not give me a sense of trust and even provenance is something that maybe provenance what you stated is more towards what I meant this is something you need to clarify further. Non-degregation is something that we cannot ensure. Not a single system can do it and especially not even in code and we're here with natural language so that is impossible. This is why like signaling or detecting possibilities of degradation is much more feasible. The reviewability part is something that definitely can be accomplished, I believe. And this can be facilitated through templates, structure on how a document looks like so that the user or even agents are getting used to on how to review it. And even skills,
 and like some procedural generation on how to verify it, that can give a sense of reviewability. And I would say reviewability is one of those things the system should strive for to make it as easy as possible for the user, but it is something that it can never guarantee. What can be done, for example, is to maybe this is even part of the config that we can set, like how the agent should frame and what kind of language it uses. So there's definitely one way to document things that needs to be precise and true and can be very technical. But helping the user to review a document,
 you can create a second set of document or even like in the conversation with the AI to match it to a simpler language or language that fits the mental model of the user. This is definitely something that the system can strive for by stating the correct mental model for explanations in the config, for example. 
Q9: I think we need to distinguish between different modes of operation. So first when we talk about AFK, so the user is not in front of the laptop and like a scheduler or nightly runs a specific task for which it uses like cloud-p or even some local model.
 What comes out is probably a report and that report should be ingested into a conversation when the user comes back to the laptop in the morning for example. I wouldn't go as far as to put into scope that everything runs automatically
 and identically in the background. That is maybe a North Star but not something that I can see right now. Then when the user is in the loop, then this is something, so this is like maybe a skill-based workflow that comes with it. There are two or three core skills that is introduced to the user to use it alongside their other skills, and those skills make sure that some of the core mechanisms
 like the steering query are being executed, and maybe this way we don't even need NMCP server, right? So it's more active, and using skills, we have discoverability, even though it's non-deterministic, and yeah, maybe even like with cloud code, you have the rules, so we can even use ADRs and simlink them as rules
 and steering, so just saying that work order as the contract is a bit difficult. 
Q10: ok


---

## Assistant  ·  2026-08-26T14:05:41.476Z

Q9 reopens a decision that `CONTEXT.md` and issue #32 both treat as settled, so I need to flag that rather than quietly absorb it. First, facts — which fields OKF actually gives you to signal with:


---

## Assistant  ·  2026-08-26T14:05:44.359Z

`[tool] Bash — Find OKF document-level frontmatter fields`


---

## Assistant  ·  2026-08-26T14:05:45.634Z

`[tool] Bash — Read OKF staleness and verification semantics`


---

## Assistant  ·  2026-08-26T14:06:24.761Z

`[tool] Bash — Check Claude Code settings and rules symlinks`


---

## Assistant  ·  2026-08-26T14:08:25.044Z

Recorded, and one place where I over-claimed.

| | |
|---|---|
| **Q7** | **Negative space.** No coined term for the other family. Bare "harness" is *ambiguous by declaration* and triggers clarification; prose always says `markdown-harness`. |
| **Q8** | Full trust is **impossible** — the Guarantee is weaker and specific. Tiers below. |
| **Q9** | **Two modes.** AFK → a **report** ingested into a conversation next morning. In-loop → **2–3 core skills** the user runs alongside their own. MCP possibly unnecessary. Work-order-as-contract too rigid. |
| **Q10** | Accepted. Budget of 1 (`yaml`), MCP hand-rolled *if built*, globs via Node built-in with semantics pinned in the corpus, count stated in the README, additions need an ADR. |

**Q7 needs one fallback you didn't specify.** "Required to clarify" works with a human in the room. In AFK mode there's nobody to ask. So the rule needs a second half: *when clarification is impossible, an agent must not guess — it names both readings in its output and proceeds with the one its current file path implies.* Inside this repo that's `markdown-harness`; inside an adopter's repo it's the Agent-host reading. Also, `CONTEXT.md` currently **defines** bare "Harness" as "the package an adopter installs" — that entry has to be rewritten, or it contradicts the decision you just made.

**My concession on Q8: you're right and I overstated it.** I wrote "nobody can quietly loosen the rules." That's prevention, and prevention is not available — a human with commit access can always relax a Rule, and #32 already conceded the same thing about archgate's suppression. The honest claim is **detection**: a loosening is always *visible*. The monotonicity mechanism survives intact, because visibility is all it ever delivered.

## OKF already has the vocabulary you're reaching for — and it separates three things you merged

You asked me to clarify provenance. The pinned spec does it better than I did. **§1, lines 36–43** enumerates five distinct questions:

> 1. What was this created from, and how was it verified? (**provenance**) 2. How much should I trust it? (**trust**) 3. Is it still true? (**freshness**) 4. Is it the current version? (**lifecycle**) 5. Was this number produced the way we said it must be? (**attestation**)

Your "signal" is **trust + freshness**, and those are orthogonal — a document can be fresh and unverified, or verified and long expired. OKF gives each its own machinery:

- **Provenance** — `sources[]`, `generated: {by, at}` (§5.1)
- **Trust tier** — a defined term: *"a level derived from a concept's `verified` field: **unverified, machine-confirmed, or human-reviewed**"* (§3, §5.3)
- **Freshness** — `stale_after`, *"an absolute instant… stale when `now >= stale_after`. An absolute instant, not a relative TTL, keeps the staleness decision a plain comparison with no reference to when the concept was read"* (§5.5)
- **Lifecycle** — `status`; absent ⇒ `stable` (§5.4)

**And here is the split that matters most for your three-months-old scenario.** OKF has **declared** staleness only. There is no document-level "last modified" field — `last_modified` lives *inside* `sources[]` and describes the source, not the document. So:

- *"My `stale_after` has passed"* → in the file, OKF-native, visible to any consumer.
- *"Nobody has touched me in 94 days"* → **not an OKF field.** Derivable from git — which you now require — but invisible to a reader unless you invent a non-OKF key.

**The architectural consequence, and I think it's the most important thing in this session.** §5 states *"their absence carries meaning… never rejected"*, §11 forbids consumers rejecting, and §10.5 assigns the behaviour to the **consumer**: *"warn or refuse when `now >= stale_after`."* OKF has already decided that the signal travels **in the file**, to whoever reads it — not through a linter's report. Which means:

> **markdown-harness does not tell you a document is stale. It guarantees the document can tell you itself.**

That is why an agent three months later gets the warning even though it has never heard of this product. It also means the harness must stay *out* of the read path — a signal only visible by running `mh check` is not a signal.

## Two contradictions I have to surface rather than absorb

**1. MCP.** `CONTEXT.md` line 3 and #32's Outcome both say "a CLI **and MCP steering API**", and #32 calls it *"the product's headline feature… the whole argument for a CLI."* Q9 says maybe you don't need it. That's a settled record being reopened — flagging per `docs/agents/domain.md`. I think it resolves cleanly, though: #32 settled the **capability** (the steering query). MCP was only ever one **transport** for it. Separate those two and nothing is actually contradicted. See Q12.

**2. "ADRs symlinked as rules" conflates two artifacts.** This repo really does that — `.claude/rules/gen-001-adr.md → .archgate/adrs/GEN-001-adr.md`. But those ADRs govern *building markdown-harness*, and an adopter must never receive them. What an adopter's repo gets is a steering document generated from **their own config**. Same mechanism, different artifact, and confusing them would ship your internal governance to strangers.

---

❓ **Q11** — **Adopt OKF's split, and decide what the signal tier actually guarantees**: Given declared ≠ observed staleness, the Guarantee's second tier is one of:

**(a) Declared only** — the harness checks `generated`/`verified`/`stale_after` are present and well-formed per the winning Rule. Whether a 90-day horizon was *sensible* is the author's problem.
**(b) Declared + policy** — the **config declares a freshness horizon per path** ("research: 90 days · design decisions: never · reference: 180 days"), and the harness checks `stale_after` against `generated.at + horizon`. A document whose declared expiry disagrees with policy is a violation.
**(c) Declared + observed** — additionally report git-observed staleness, which cannot enter frontmatter without inventing a non-OKF key.

➡️ **(b) is the Guarantee; (c) is report-only and never enters a file.** Two reasons. First, (a) is where the signal turns into noise: if an agent picks `stale_after` freely, every document claims 90 days and the field stops meaning anything — the policy has to live in the config, which is the one place a human reviews. Second, (c) in frontmatter would make your signal invisible to every other OKF consumer, which throws away the interop that made OKF worth choosing. Keep files OKF-pure; keep observed staleness in the report, where the audience is you and not a future agent.

Note what (b) unlocks: **`stale_after` becomes deterministic and derivable** — `generated.at + horizon`, no model, no judgment. That may be a better *first* automation than index generation, which duplicates an OKF first-party function. And the reviewability loop gets a natural mechanism: a document past `stale_after` generates a **verify** work item, and `verified: {by: human:han, at: …}` is the human's signature raising the Trust tier. That's your review budget, expressed in fields OKF already defines.

---

❓ **Q12** — **How does steering actually reach an agent, and is MCP in v1?**: The channels, and the property that decides it:

| channel | reliable? | portable across hosts? |
|---|---|---|
| MCP tool call | **no** — model elects to call it | needs MCP support |
| Skill | **no** — model elects to fire it | needs skill support |
| Generated steering doc loaded as context/rules | **yes** — always present | needs only "reads a file" |
| Event hook on write | **yes** — fires deterministically | host-specific, differs per host |
| CLI invoked by a skill or rule | as reliable as its trigger | needs only "runs a command" |

You called skills "non-deterministic but discoverable" — that's exactly right, and it applies equally to MCP. **Both are model-elected. Neither is a reliable channel.**

➡️ **v1 = generated steering document + CLI. Defer MCP. Add hooks later as per-host hardening.** That combination asks the Agent host for nothing but "read a file" and "run a command", which means Codex, Antigravity and a DeepSeek host work on day one with zero adapters — and it's one code path, not two. MCP earns its place only when a host makes it the *best* channel, and by then the CLI has already defined the answer's shape, so the server is a thin wrapper rather than a second implementation.

This also means your instinct and #32 are compatible: the **steering query** stays the headline feature; MCP was a transport assumption, not the feature.

---

❓ **Q13** — **The write boundary, now concrete**: Q11(b) makes `stale_after` a computed value whose inputs are already in hand. So:

**(a) Read-only.** Report "`stale_after` disagrees with policy"; an agent writes the fix.
**(b) Derived-field writes.** The harness computes and writes values it fully derives — `stale_after` from `generated.at + horizon` — and never touches prose or any field requiring judgment.
**(c) Derived fields + wholly-owned files** (generated indexes as well).

➡️ **(b) for v1, (c) once the index question is settled — and `--check` is the default, `--write` always explicit.** This is the smallest possible carve-out from "report, never rewrite": a computed scalar, both inputs visible, byte-identical on every run. CI only ever verifies; nothing mutates without someone typing the word. Two terms for the glossary if you take it: **Derived field** (a frontmatter value the harness computes rather than reads) and **Owned file** (one whose every byte the harness authors).

---

❓ **Q14** — **Body governance: is the "mental model" idea a config field or a second Module?**: Your Q8 idea — the config states how an agent should frame things, so a technical document can be re-rendered against the reader's mental model — is genuinely new and doesn't exist in any tool the research surveyed.

**(a)** Out of scope for v1. Frontmatter only.
**(b)** A second Module with a `body:` section in v1 — templates, required headings, structure checks.
**(c)** A **register declaration** only: the config states audience and mental model per path; the steering answer carries it; nothing about the body is checked.

➡️ **(c) in v1, (b) later if (c) proves itself.** (c) costs almost nothing — it's prose the harness carries and never interprets, exactly like `intent`, so it adds no checking machinery and no new failure mode. And it's the piece that makes your workshop audience real: the config can say *"explain this to a non-technical product owner"* and every agent that asks what governs the path is told. (b) needs a markdown body parser, heading semantics, and a whole new violation family — that's the second Module, and it's a project, not a feature. Ship (c), watch whether declaring register visibly changes what agents write, and let that decide whether (b) is worth building.

---

❓ **Q15** — **The October cut line**: You have 3–4 workshops in October–November and it's 2026-08-26 — call it six weeks to a shippable artifact. I need three facts only you have: **when is the first one, how long is a session, and what does a participant have to do with their own files to walk out convinced?**

➡️ My proposed cut, for you to shoot at. **In:** `mh init` (writes a config from a preset, and reports zero violations on their existing tree because governance is opt-in) → `mh check` → `mh steer <path>` → one document demonstrating the freshness signal to an agent that has never heard of the tool. That last moment is the demo — everything else is setup for it. **Out:** MCP, index generation, body templates, AFK scheduling, the loosening check. **The riskiest item is none of the above** — it's `mh init` on a stranger's Windows laptop with git absent. That's the one that ruins a workshop, and it's the one thing on this list nobody has designed yet.

---

Still queued: severity tiers (**contested** — preference 5 says all rules `error`; OKF forbids consumers rejecting on 6 of its 16 MUSTs) · suppression · report format · the config filename · the AFK report format and how it gets ingested · which 2–3 skills ship · CI/CD gates and whether this publishes to npm · and which documents this session writes.


---

## Human  ·  2026-08-26T14:44:30.435Z

Q11: b
Q12: I need some clarification here. Hooks would be something we need to set up for each of those harnesses individually. And an MCP server, even locally, would technically work for every harness because MCP is a third-party standard. The thing is that hooks are harness specific and we would need to provide an integration layer to able to set up the hook for the user. I just checked like with those major providers I had in my head like Collard, Antigravity Codex, DeepSeek, they all provide hooks. So that is one way to go.
 But as my target audience are potentially non-technical people, what about like Claude Code Desktop, for example, sorry, Claude Desktop, for example. Hooks would be a great deterministic way to ensure that the API from us is being called. And what I thought about how we can build upon is maybe that every single time a document is read, we sort of count it somehow, right? So every time it is updated, every time it is read,
 some tracker profile as metadata that can help an LLM to judge the impact of the file. So if a file is like read every single time, like five times a day, even maybe after one week, we should kick in a signal that this might become, it's already stale because it has a bigger impact. As a side note, I maybe thought of like in the direction of MCP, if we would disallow read and write privileges for Cloud Core, for example, and only run it through our MCP, that would be a way to enforce it. But this is not something if behavior you can scale to different harnesses and environments, I guess. So all in all, I really like the idea of hooks. Just need to check that how we can make it available and integrate it. I am not against it if we cannot integrate to all the harnesses out there. Limiting to a specific set for now to make it work is a good start. 
Q13: I would agree with B for version 1 because this is a baseline mechanism that can work deterministically. However we should decide or maybe it's a configurable thing what the action is if it is stale. Like can the user just simply reconfirm it or is there a skill that helps the user to reconfirm and review it? Is there a skill that launches the same or a updated research
 task to check if it's still up to date and we have LLM judge behind it? So the signal itself I would say is something we can start with as I mentioned in Q12 if we are able to somehow detect the readings and the updates in some form of metadata that can also generate the signal but the action needs to be decoupled and configurable in my opinion. 
Q14: I would say this is out of scope and a second module as this is really more focused on the body and its configuration should be handled separately. The option to define a mental model for a specific path is something I want to keep open. So that can be added in later, I guess. So the second module is maybe more towards reviewability. 
Q15: Actually, the first deadline is the 15th of September, where I have a presentation slot in our company's next AIR hand, where I want to introduce this system. And your cut, the init, check and steer, I think is already enough for that. Once that has been implemented, we can define the scope for the next part before the workshops. 

As a side feedback, I think we're drifting more towards details and drifting away from the vision for product and architecture. Let's refocus on those as they are more higher level. The decisions, so the implementation and feature decisions that we talked about, I definitely want to keep, but maybe not document them as final decisions yet. Really the outcome here is to document the vision that has helped the agent and me to derive decision makings in the future.
 and one for architecture.


---

## Assistant  ·  2026-08-26T14:46:46.930Z

Accepted — we've gone below the altitude you need. Recording the last round, answering Q12 as a clarification rather than re-asking it, then one final round *at* vision altitude and a proposed document set.

| | |
|---|---|
| **Q11** | **(b)** — the config declares a freshness horizon per path; `stale_after` is checked against `generated.at + horizon`. |
| **Q13** | **(b)**, and **signal ≠ action**. The signal is v1; what happens when something goes stale (reconfirm · guided review · relaunch the research with an LLM judge) is **decoupled and configurable**. |
| **Q14** | Out of scope. **Module 2 is reviewability** — body, structure, and the mental-model-per-path option kept open for it. |
| **Q15** | **15 September 2026**, company presentation slot. init/check/steer is enough. Next scope defined after. |

**That's 20 days, not six weeks** — and it's a presentation, not twenty strangers' laptops. Which retires the risk I called worst: `mh init` on an unknown Windows machine isn't on the critical path until the workshops.

## Q12, clarified

They aren't two answers to one question — they answer different questions, which is why it felt unresolvable.

- **MCP answers *reach*.** Universal, third-party, and the only channel that reaches **Claude Desktop** — which has no hooks, because it isn't an agentic CLI. But a tool call is **model-elected**: you cannot guarantee it happens.
- **Hooks answer *guarantee*.** Deterministic, and you're right that all four majors have them — but each needs its own integration layer, and Claude Desktop has none.
- **Your "disallow read/write, force it through our MCP" idea is the missing piece**: that's *permission-based* enforcement, and it's the only way to make MCP deterministic. It works in Claude Code and doesn't generalise — so it's a hardening option per host, not an architecture.

**The clarification that actually matters is that neither one carries the floor.** Because the signal lives in the frontmatter, an agent that never calls your tool — no hook, no MCP, no skill — still reads `stale_after` when it reads the file. Hooks and MCP raise the **ceiling** (steering at write time, usage tracking). They don't hold up the **floor**. That's why a non-technical Claude Desktop user gets the core value with zero integration, and it's a vision-level statement, not a feature one.

**One warning on the read-counting idea, because it cuts against a principle we derived.** Counting reads puts you *in* the read path, and writing a counter into frontmatter on every read would mean the file changes constantly — git churn, merge conflicts, and the end of "the diff is meaningful", which is the property the whole trust argument rests on. The resolution: **usage accumulates in a local sidecar ledger, and only a periodic summary folds into frontmatter.** Observation stays optional and host-specific; the signal it produces stays universal and in-file. Worth noting OKF already has `usage_count` and `usage_window` — but scoped to `sources[]`, describing a source's usage, not the document's. Document-level usage is yours to invent, and it's the one place you'd be extending OKF rather than adopting it.

## What I'll produce

Two vision documents, not decision records. Feature decisions from this session get **kept but marked provisional** — my recommendation is a single tracking issue in this repo, the way #32 works, linked from `AGENTS.md`, so nothing lands in `docs/design-adr/` or `.archgate/adrs/` that we agreed isn't final yet.

---

❓ **Q16** — **Who is v1 for, in priority order?**: "Non-technical" has been doing a lot of work in this conversation, and Claude Desktop exposed that it means two different people.

**(a) Engineer-adjacent first** — floor is an agentic CLI + git + admin rights. Non-technical staff arrive later.
**(b) Non-technical first** — Claude Desktop and MCP, no CLI, no visible git.
**(c) Two roles, explicitly, from day one** — an **operator** who installs and owns the config (technical, that's you in every workshop), and a **contributor** who only writes documents and never sees the config.

➡️ **(c).** It's already what happens: you set it up, they use it. And it makes the config's audience explicit, which is the whole point of `intent` and the register idea — the config is one technical person writing *for* non-technical readers. The contributor's path then needs no hooks, no CLI and no MCP: they write, the operator's config governs, and the signal is in the file. That's how you get a non-technical story without building a non-technical product.

---

❓ **Q17** — **What will this never be?** A vision document is defined as much by its exclusions, and yours are unusually clear from the session. My candidate list — tell me which to strike and what's missing:

Never runs a model or holds an API key · never a server, SaaS, or hosted service · **never phones home** · never a vector database or RAG pipeline · never an Agent host (no tools, no permissions, no session) · never required in the read path for the signal to work · never rewrites prose a human wrote · never enforces a spec it doesn't own — OKF stays a Preset · **never has a UI.**

➡️ Keep all of them, and the last one is the interesting one: **the Agent host *is* the UI.** That's the sentence that explains why a governance tool for non-technical people ships as a CLI, and it turns your biggest apparent weakness into the position. Strike it only if you think you'll want a UI within a year — because if so, the architecture should know that now.

---

❓ **Q18** — **Is this a business, an open-source tool, or your own system that others may use?** I never asked, and it's genuinely vision-level: it decides the README's register, whether the config language is a published spec or an implementation detail, whether the OKF preset is a community artifact, and what "adoption" means as a success measure. You opened with "one personal and one potentially a business."

**(a)** Open source, MIT, no commercial intent. The business is *your* consulting, not the tool.
**(b)** Open core — tool is free, something around it is paid later.
**(c)** Deliberately undecided, license permissively now to keep both open.

➡️ **(a) for now, stated plainly, with (c) as the honest hedge — permissive license, no commercial machinery, and the vision document says so.** Reason: your revenue is the workshops and the enablement work, and the tool's job is to make those credible. A tool that's visibly free and self-hostable is *worth more* in that role than one with a pricing page, and every hour spent on monetisation is an hour not spent on the 15 September slot. But say it in the document — an unstated business model gets assumed, usually wrongly.

---

❓ **Q19** — **Are these the architecture tenets?** Distilled from the whole session. These are what an agent would consult in six months to derive a decision we never discussed:

1. **Deterministic core, delegated judgment.** The harness never guesses; the Agent host never governs.
2. **The signal lives in the file.** The harness keeps frontmatter true; it is not the channel the signal travels through.
3. **The check path is fast, offline, and side-effect-free** — no network, no git call, no model.
4. **The contract is the portable artifact** — config language + report format + fixture corpus. The TypeScript is one implementation of it.
5. **One config, first match wins, nothing merges.**
6. **Governance is opt-in by path.** A fresh install reports zero violations on an existing tree.
7. **The dependency budget is a number in the README**, not a preference.
8. **Writes are derived-only and never default.** `--check` unless told otherwise.
9. **Two modes.** In-loop is interactive and skill-driven; AFK produces a report a human ingests as conversation.
10. **Detection over prevention.** Nothing can be stopped; everything can be made visible.

➡️ I'd add an **11**: *integration is layered — the floor requires nothing of the Agent host but reading a file; each additional guarantee (steering at write time, usage tracking) is an optional host-specific layer that can be absent without breaking the floor.* That's the Q12 clarification promoted to a tenet, and it's the one that keeps Claude Desktop and Claude Code in the same product. **Anything you'd strike, or a tenet I've missed?**

---

**Proposed deliverables**, for confirmation before I write anything:

| file | what it is |
|---|---|
| `docs/vision/product.md` | The promise, the two roles, the Guarantee tiers, exclusions, horizons (15 Sep → workshops → beyond) |
| `docs/vision/architecture.md` | The tenets, with the reasoning that produced each |
| `README.md` | Front door. New file — neither repo has ever had one |
| `AGENTS.md` | Updated: points at both vision docs, the bare-"harness" rule, and the provisional-decisions issue |
| `CONTEXT.md` | **Glossary only** — new terms settled this session, no decisions |
| one GitHub issue | Provisional feature decisions, explicitly not final |

Two placement questions inside that: `docs/vision/` is a new directory — say if you'd rather these sit at the root or under `docs/`. And `CONTEXT.md`'s **Harness** entry has to be rewritten regardless, since it currently asserts the single meaning your Q7 answer retired.


---

## Human  ·  2026-08-26T15:04:54.846Z

Q16: c, then a
Q17: I will point out the ones I'm thinking we might include or where I want to discuss. Never rewrites prose a human road. Soft. Yes. Needs clarification. The idea is that we provide metadata, structure and governance for an agent to rewrite it better (e.g. in the mental model of the user). So there's that. Never enforces a spec it doesn't own. Also, I'm not 100% sure if this project imports some ADRs from ArchGit, for example, which can help enforcement. Not sure if that is the case. Otherwise, we haven't talked about
 adaptability to other systems, but we can exclude that from the scope now. So I would say a soft agree. UI is something that I will most likely include but in a sense of making the configuration easier for a non-technical user so we would not we will not have our own TUI or our own graphical user interface for a chat but maybe just a standalone
 small web application that helps configure because I imagine our configuration file can be a bit daunting for non-technical users. 
Q18: a but open to something around it is paid later, a final benefit is that i may contribute to my CV appliying for AI jobs
Q19: Same here, I'm going to point out the ADRs (not tenent) and give my feedback. First, deterministic core 100% correct. Not sure about the delegated judgment and also the agent host never governs. I don't think that is correct. So I would rather say deterministic core providing agent steering that assists the host because they might have also governing mechanisms. Question here, by the way, you're already using agent hosts. And I think with this, you're referring to like cloud code or anti-gravity, for example. So what I would still in my mind call harnesses. Maybe we can settle on the term host harness, does that make sense? I like the idea so I would withdraw my earlier idea to count reads and writes maybe in a separate source like a metadata file or SQLite that would introduce all kinds of caching problems so signal lives in the file I would agree 100% but the statement it is not a chance
 the signal travels through needs more clarification I would say because the steering query is on the path when a host harness is processing so not sure how we need to word it. yes to number three, provided that it can also run in a pipeline, So the word offline maybe should be replaced with local or something like more contained. Yes to five. I think that is part of the research result from the previous repository. Yes, same as number five, it's part of the research. For five and six, there should be some reference to the research to anchor it. soft no to seven i think a hard budget makes no sense as we don't know the scope how this project will develop yet for now i will probably settle with a guideline in a adr in archgate to discourage the free download of any dependencies and state like
 the way we are evaluating so that the agent can make the right call and research. 8. You need to explain a bit more. I'm not really sure what you mean with writes are derived only. I wouldn't settle on that yet and I also would not call it two modes but definitely there are ways of using it while a human sits in front of a laptop and also another way to consume this harness in an AFK. Modes sounds right but the whole 0.9 feels like a bit too early to save it as a ADR. Agree to 10. Agree to 11 of layered architecture and integration.


---

## Assistant  ·  2026-08-26T15:05:57.581Z

Good, dense feedback. Two fact-checks I owe you — the research anchors for tenets 5 and 6, and whether a config editor is actually feasible against our one dependency:


---

## Assistant  ·  2026-08-26T15:06:00.413Z

`[tool] Bash — Verify research citations for tenets 5 and 6`


---

## Assistant  ·  2026-08-26T15:06:02.806Z

`[tool] Bash — Test whether yaml preserves comments on programmatic edit`


---

## Assistant  ·  2026-08-26T15:06:18.809Z

`[tool] Bash — Verify first-match-wins precedent citation`


---

## Assistant  ·  2026-08-26T15:07:57.613Z

Four clarifications you asked for, verified anchors, and one measured finding that the UI decision creates.

## The clarifications

**"Host harness" — yes, adopt it.** It's better than my "Agent host" for a reason that matters: it keeps the industry's word, so nobody has to unlearn anything, and it's a *qualified* form, which is exactly what your Q7 negative space requires. Bare "harness" stays ambiguous and triggers clarification; both referents carry a qualifier — `markdown-harness` and **host harness**. It's also self-explaining: it hosts the agent — model, tools, permissions, session. One cost: it's a coinage nobody else uses, so it needs a one-line gloss on first use ("*the agentic CLI you already run: Claude Code, Codex, Antigravity*"). Switching to it from here.

**Tenet 2 — you're right, I conflated two paths.** Reworded: *markdown-harness is on the **authoring** path (steering before a write, checking after) and never required on the **consumption** path. A reader that has never heard of it still sees `stale_after`.* The steering query is emphatically on the authoring path — that's the headline feature. What must never be true is that reading a governed document requires our tool.

**Tenet 8 — it was two claims wearing one label, which is why it read badly.** (i) *Derived-only*: only write values computable from inputs already in hand — `stale_after = generated.at + horizon` — never prose, never a body. (ii) *Never default*: `check` reports and never mutates; mutation needs an explicit command. The second is the one worth being a tenet, because if `check` can modify files then a green check stops meaning "your repo was already correct" and starts meaning "it is correct now, after I changed it" — and only the first is evidence. So: **keep (ii) as the tenet, let (i) ride with the Q13 decision instead of being promoted.**

**Archgate: nothing is imported and nothing ships.** #32 settles it — *"archgate is retained but **internal only** — dev governance over `markdown-harness`'s own source. Never shipped, never carrying OKF rules."* Current state matches: `.archgate/adrs/` holds only `GEN-001`, symlinked into `.claude/rules/`, and `.archgate/` is excluded from both `tsconfig.json` and `eslint.config.mjs`. So the exclusion is safe — but its **wording should change**, because what you actually mean is already settled in `CONTEXT.md` and is stronger: **specs arrive as Presets, never as built-in behaviour** — "deleting a Preset changes nothing except which Rules run." And "adaptability to other systems" isn't an exclusion at all; the Preset mechanism *is* that adaptability, just unexercised in v1.

## Anchors for 5 and 6, verified in the tree

**First match wins** — `parts/markdownlint-obsidian.md:185` ("first match wins, not last, not most-specific"), `frontmatter-linter-config.md:159`, `pathrule-precedence.md:570`. And the two failures it avoids: Jekyll's `has_precedence?` ranking by **`new_path.length`**, the byte length of the scope string (`frontmatter-linter-config.md:129`, source at `parts/ssg-family.md:181-208`), and `path_is_subpath?` being a bare `path.start_with?(parent_path)`, so `docs/log` silently captures `docs/logging/` (`frontmatter-linter-config.md:226-227`).

**Opt-in by path** — `"default": false` is proven prior art, defaulted the wrong way: *"When no configuration object is passed or the optional `default` setting is not present, all rules are enabled"* (`parts/markdownlint-obsidian.md:547-551`). **Keep the key, flip the default.** Obsidian Linter has no such key "and is correspondingly unusable in default-ignore mode."

## ⚠️ The config UI creates a load-bearing decision, and I measured it

Good news: `yaml` round-trips **comments** through a programmatic edit — I just verified it on a config-shaped document, comments preserved above and inside the rule list. So a config web app needs **zero new dependencies**.

Bad news, same test: it reformatted `[docs/**/*.md]` → `[ docs/**/*.md ]`. Comment-safe, **not byte-exact**. Which means a UI that saves a config produces diff noise — and the config diff is precisely the human's review surface for detecting a loosening (tenet 10). **Noise in that diff attacks the detection mechanism directly.**

So if a config UI is on the roadmap at all, the architecture needs a **canonical serialization** decided now: one formatting the tool always emits, normalized on every write, so a UI edit and a hand edit produce the same bytes. Cheap today. Painful once adopters have configs you'd have to re-normalize under them. This is the second thing this session has surfaced that's cheap now and expensive later — the first being constraint-key monotonicity.

## Revised, for your sign-off

**Tenets** — 1 reworded to your correction (*a deterministic core providing steering that assists the host harness, which has governance of its own*), 2 reworded above, 3 "offline" → **hermetic** (no network, no model, no external service; same tree in, same result out — which is *why* it runs identically locally and in a pipeline), 7 → **dependencies are admitted by a stated evaluation procedure, not a count** (goes in an archgate ADR: silent-failure → buy; loud and under 100 lines → build; check Node's stdlib first; stars/recency as a screen, never a criterion), 8 narrowed above, 9 **described, not decided**, 10 and 11 as written. **Read/write counting: withdrawn, recorded as considered-and-dropped** (sidecar caching, and it would have put us on the consumption path).

**Exclusions** — "never rewrites prose" → *the harness never rewrites prose, and exists to help an agent rewrite it well*; "never enforces a spec it doesn't own" → *specs arrive as Presets, never as built-in behaviour*; "never has a UI" → **struck**, replaced with *no chat UI, no TUI, no runtime UI; a config-authoring web app is in scope later*.

---

❓ **Q20** — **Tenet 4, which you skipped**: *the contract — config language, report format, fixture corpus — is the portable artifact; the TypeScript is one implementation of it.* Substantively you settled this in Q5, but you didn't confirm the tenet wording.

➡️ Keep it. It's the tenet that makes your non-Node north star reachable without a rewrite, and its only cost today is declaring the fixture corpus a spec rather than a test suite.

---

❓ **Q21** — **Is the personal second brain a supported v1 case, or the origin story?**: You opened with it, and it's the most emotionally load-bearing thing in the session — but an Obsidian vault has no git, no CI and no code, and Q16 put engineer-adjacent first.

**(a)** Origin story only. The README tells it as motivation; v1 doesn't claim to serve it.
**(b)** Supported v1 case — a vault is just a repo, and you'll `git init` yours.
**(c)** Supported, and the *primary* dogfood — you run it on your own vault from day one.

➡️ **(c), and say so.** Not for feature reasons — for evidence. A governance tool whose author doesn't govern their own knowledge base with it is not credible in a workshop, and you already know the failure mode intimately enough to detect it recurring. It also costs nothing: your vault becomes a repo, which Q2 already required.

---

❓ **Q22** — **What does success look like 12 months out?** Sept 15 and the workshops are known. A vision document needs the horizon past them, because that's what future decisions get derived against.

**(a)** Adoption — N people outside your orbit using it.
**(b)** Evidence — one knowledge base you have trusted continuously for a year without it degrading.
**(c)** Credibility — the artifact that makes the enablement work land, plus the CV benefit you named.

➡️ **(b) as the measure, (c) as the payoff, (a) as a lagging indicator you don't chase.** (b) is the only one that tests the actual thesis, and it's the one you'd notice being false.

---

❓ **Q23** — **Is OKF a headline or a footnote?**: You chose it because v0.2 has the fields that make the trust signal possible. But "built on Google's Open Knowledge Format" is also a credibility asset for a non-technical audience — and simultaneously a liability, since upstream mutates in place under a fixed label and publishes no releases.

➡️ **Headline in the positioning, footnote in the architecture.** Say it early — it's true, it's third-party validation, and it explains why the vocabulary looks the way it does. But the vision document must state in the same breath that OKF ships as a **Preset**, is pinned to a vendored revision, and that the product outlives any spec it carries. That way "Google" earns you attention without becoming a dependency you can't control.

---

Answer these four and I'll write: `docs/vision/product.md`, `docs/vision/architecture.md`, `README.md`, the `AGENTS.md` update, `CONTEXT.md` glossary additions, and the provisional-decisions issue.


---

## Human  ·  2026-08-26T15:14:30.146Z


Q20: ok
Q21: c, generally a llm-wiki is a primary use-case, which i would like to demonstrate in the workshop.
Q22: ok
Q23: ok

Do not start writing yet, wait for my instructions


---

## Assistant  ·  2026-08-26T15:15:19.759Z

Recorded. **Nothing written — holding for your instructions.**

## What Q21's second half changed

You didn't just confirm (c) — you promoted **LLM-wiki from origin story to primary use case, and made it the workshop demo.** That's a bigger move than it looks, and three things follow:

1. **It changes who the documents are *for*.** In an LLM-wiki the primary reader is a model, not a person. So frontmatter signals, index files and progressive disclosure stop being hygiene and become *the interface*. It also inverts your Q8 register idea: the LLM reads the precise technical version, and the **human** is the one who needs the simplified rendering. Same feature, opposite direction from how we discussed it.
2. **It sharpens the demo past "look, a linter."** The moment that lands in a room is: *an agent discovers a document it has never seen, reads its own signal, and correctly declines to treat a three-month-old decision as current.* That's demonstrable in about ninety seconds and it's the entire thesis.
3. **The demo needs a corpus, and you already have one.** `fixtures/` is a synthetic repo root with 15 governed files across seven path families. It was built to prove the config vocabulary; it can double as the seeded demo wiki. That's a non-code deliverable for 15 September that costs nearly nothing.

One spelling note before it reaches a README: **Andrej Karpathy**, and the concept reads as **LLM-wiki** (you wrote "LRM" the first time). If "LLM-wiki" is going to be a named use case, it needs a `CONTEXT.md` entry — it's currently a term we've both been using without a definition.

## The frontier is empty

Every branch we opened is settled or deliberately deferred. Consolidated, for you to confirm or correct:

**Product** — The promise is **trust**, and full trust is impossible, so the mechanism guarantees something specific instead: conformance as the low tier, and a **signal** the document carries about its own freshness and verification. OKF supplies the vocabulary — provenance, trust tier, freshness, lifecycle — and the config supplies the policy. Two roles: an **operator** who owns the config, a **contributor** who only writes. Engineer-adjacent first, LLM-wiki as the primary use case, your own vault as the dogfood. Free and permissive, no commercial machinery yet. Success at twelve months is one knowledge base you've trusted continuously without it degrading.

**Architecture** — Eleven tenets as revised: deterministic core providing steering that assists a **host harness** which has governance of its own; the file carries the signal, and we're on the authoring path but never required on the consumption path; a hermetic check path; the contract as portable artifact; one config, first match wins, nothing merges; opt-in by path; dependencies by stated procedure; no mutation without an explicit command; two ways of using it (described, not decided); detection over prevention; layered integration where the floor asks the host harness for nothing but reading a file.

**Load-bearing now — the four cheap-today, expensive-later items**, which was your original ask:

1. **Constraint-key monotonicity** — every constraint key needs a declared loosening direction, and `pattern` is undecidable and must flag unconditionally. Without it the detection tenet ships broken-but-certifying, which is worse than absent.
2. **Canonical serialization of the config** — forced by the config UI, because diff noise attacks the same review surface detection depends on.
3. **The fixture corpus declared a spec**, not a test suite — the only thing that makes a future port verifiable.
4. **Glob semantics written into that spec** rather than inherited from whichever library or runtime happens to implement them.

**Provisional, kept but not final** — freshness horizons in config · derived `stale_after` · steering channel (generated doc + CLI first, MCP deferred, hooks as per-host hardening) · staleness *action* decoupled and configurable · Module 2 as reviewability · mental-model-per-path · severity tiers, still contested · suppression · report format · config filename · the AFK report and how it gets ingested · which 2–3 skills ship.

**Withdrawn** — read/write counting, and the reasons it was dropped.

Standing by. When you're ready, tell me what to write and in what order — and whether `docs/vision/` is the right home or you'd rather those two documents sit somewhere else.


---

## Human  ·  2026-08-26T15:16:48.676Z

<command-message>writing-for-agents</command-message>
<command-name>/writing-for-agents</command-name>
<command-args>use this skill to author the documents. Dont commit</command-args>


---

## Human  ·  2026-08-26T15:16:48.676Z

Base directory for this skill: /Users/han/Developer/markdown-harness/.claude/skills/writing-for-agents

Reference for writing any document an agent consumes: a skill, an `AGENTS.md` / `CLAUDE.md`, a doc reached by a pointer. The packaging differs; the writing does not: the same levers make each one predictable, since the agent takes the same _process_ every run rather than producing the same output.

When the document you're writing is a skill, read [`SKILL-MECHANICS.md`](SKILL-MECHANICS.md) for frontmatter, invocation choice, and router skills.

## Context pointers

A **context pointer** is a reference held in the agent's context that names some out-of-context material and encodes the condition for reaching it. A skill's description is one; a line in `AGENTS.md` naming a doc is the same object. The pointer's _wording_, not its target, decides when the agent reaches the material, and how reliably. A must-have target behind a weakly worded pointer is a variance bug: sharpen the wording first, and inline the material only if sharpening fails.

A pointer does two jobs: state what the material is, and list the **branches** that should trigger reaching it (a branch is a distinct case the document handles, so different runs take different paths through it). Every word of an always-loaded pointer costs on every turn, so it earns even harder pruning than the body:

- **Front-load the leading word**: the pointer is where it does its triggering work.
- **One trigger per branch.** Synonyms that rename a single branch are one branch written twice; collapse them and keep only genuinely distinct branches.
- **Cut identity the body already carries.**

## The two loads

Every document and pointer you add spends one of two budgets:

- **Context load** is the cost of always-loaded material on the agent's window: an `AGENTS.md` line, a skill description, anything sitting in context every turn, spending tokens and attention whether or not it fires.
- **Cognitive load** is the cost on the human: which documents exist and when to reach for each. The human is the index. Not a cost to minimise: it is the price of human agency; spend it where human judgement matters, remove it where it does not.

Material reached only through a pointer escapes context load at the price of the pointer's own line; material with no pointer at all rides entirely on cognitive load.

## Information hierarchy

A document is built from two content types: **steps** (the ordered actions the agent performs) and **reference** (definitions, rules, facts consulted on demand). The two mix freely: all steps (a recipe), all reference (a review's rules, this skill), or both. The core decision is where each piece sits on the **information hierarchy**, a ladder ranked by how immediately the agent needs the material:

1. **In-file step** is the primary tier: what the agent does, in order.
2. **In-file reference** is consulted on demand. Often a legitimately flat peer-set (every rule of a review on one rung), which is a fine arrangement, not a smell.
3. **Disclosed reference** is pushed out into a separate file, reached by a context pointer, loaded only when the pointer fires. Spans a sibling file in the same folder through fully external reference that lives anywhere and any document can point at.

Push too little down and the top bloats; push too much and you hide material the agent actually needs. That tension is the whole decision.

**Progressive disclosure** is the move down the ladder (out of the main file and behind a pointer) so the top stays legible. Not primarily a token optimisation: it is how the hierarchy is protected. Branching is the cleanest disclosure test: inline what every branch needs, and push behind a pointer what only some branches reach. When a document has steps, in-file reference that should be disclosed buries them and turns attending to them into a coin-flip: a variance lever, not just a legibility one.

**Co-location** is the within-file companion: where the ladder decides _how far down_ a piece sits, co-location decides _what sits beside it_ once there. Keep a concept's definition, rules, and caveats under one heading rather than scattered, so reading one part brings its neighbours with it. The test: the document should read like documentation written for the agent. Grouped material reads that way; scattered material does not. (Distinct from duplication: that repeats one meaning in two places; scattering fragments one meaning across many.)

**Sprawl** is the failure mode here: a document simply too long, even when every line is live and unique. Attention thins across the excess, and every extra line is one more to keep relevant. The cure is the ladder: disclose reference behind pointers, and split by branch or sequence so each path carries only what it needs.

## Steps and completion criteria

Every step ends on a **completion criterion**, the condition that tells the agent the work is done. Two properties make it a lever:

- **Clarity**: can the agent tell done from not-done? A vague bound ("understanding reached") invites **premature completion**: ending the step before it is genuinely done, attention slipping to _being done_. The visible steps still ahead (the **post-completion steps**) supply the pull; the criterion's clarity is the resistance. Defend in order: **sharpen the bound first** (local and cheap); only if it is irreducibly fuzzy _and_ you observe the rush, hide the later steps by splitting the sequence. Hiding only works across a real context boundary (a hand-off or a subagent dispatch; an inline call leaves the later steps in context and clears nothing).
- **Demand**: how much it requires. "Every modified model accounted for" forces thorough work where "produce a change list" does not. Demand drives **legwork** (the digging the agent does within the work, latent in the wording rather than written as its own step), and it is not step-bound: "every rule applied" binds a body of flat reference just as "every step done" binds a sequence, which is how an all-reference document still carries an exhaustiveness bar.

The strongest criteria are both checkable and exhaustive.

## When to split

Splitting one document into two spends one of the two loads, so split only when the cut earns it:

- **By sequence**: split a run of steps where the post-completion steps tempt the agent to rush the one in front of it. Keeping them out of view drives more legwork on the current task. Beware the reverse: merging sequences exposes each step's later steps to what follows, inviting premature completion.
- **By invocation**, skill-specific: see [`SKILL-MECHANICS.md`](SKILL-MECHANICS.md).

## Leading words

A **leading word** is a compact concept already living in the model's pretraining that the agent thinks with while running the document (_lesson_, _fog of war_, _tracer bullets_). Repeated as a token, never as a sentence, it accumulates a distributed definition and anchors a whole region of behaviour in the fewest tokens, by recruiting priors the model already holds. Coining your own works if you define it clearly, but a made-up word recruits no priors: you pay in definition tokens what a pretrained word gives free; reach for an existing word first.

It anchors twice. In the body, _execution_: the agent reaches for the same behaviour every time the word appears, and inside flat reference it focuses attention on a class of thing to look for. In a pointer, _invocation_: when the same word lives in your prompts, your docs, and your codebase, the agent links that shared language to the material and reaches it more reliably.

Hunt for opportunities to refactor with leading words. A triad spelled out at three sites, a pointer spending a sentence to gesture at one idea. Each is a passage begging to collapse into a single token:

- "fast, deterministic, low-overhead" → _tight_ (a _tight_ loop).
- "a loop you believe in" → _red_, turning a fuzzy gate into a binary observable state (the loop goes _red_ on the bug, or it doesn't).

You win twice: fewer tokens, and a sharper hook for the agent to hang its thinking on. Assume every document is carrying restatements that leading words retire. Go find them.

**Negation** is the failure mode beside this lever: steering by prohibition drags the forbidden behaviour into context and makes it _more_ available, not less. _Don't think of an elephant_, and the elephant is all there is; the negation is a weak modifier the strongly-activated concept overruns, so the ban half-reads as an instruction to do the thing. Prompt the **positive**: state the target behaviour ("write one-line comments") so the banned one is never spoken. A prohibition earns its place only as a hard guardrail you cannot phrase positively; even then, pair it with the positive target so attention lands on what to do.

## Pruning

- Keep each meaning in a **single source of truth**: one authoritative place, so changing the behaviour is a one-place edit. **Duplication** (the same meaning in more than one place) costs maintenance and tokens, and inflates a meaning's prominence on the ladder past its real rank. (The accidental inverse of a leading word, which repeats a token on purpose, never the meaning.)
- The **environment** is a source of truth too (`package.json` scripts, config files, the directory layout, `--help` output), and a document that restates it is a **cache**: a copy of a lookup, earning its load only when the lookup is expensive. Cache what the agent cannot find by looking: the unwritten convention, the reason behind a choice, the gotcha no config confesses. Leave the one-file, one-command lookups to the environment, where they cannot go stale.
- Check every line for **relevance**: does it still bear on what the document does? A line loses relevance by never bearing on the task (mere exposition, or a branch that should be disclosed) or by going stale as the behaviour or world it describes changes. Shorter documents are easier to keep relevant. Without a pruning discipline the default fate is **sediment**: stale layers that settle because adding feels safe and removing feels risky, until you must core down through them to find what is still live.
- Hunt **no-ops** sentence by sentence: an instruction the model already obeys by default pays load to say nothing. The test (does it change behaviour versus the default?) is model-relative, not reader-relative: two people disagreeing about a no-op disagree about the default, and settle it by running the document, not by debate. When a sentence fails, delete the whole sentence rather than trim words from it. The test also grades leading words: a word too weak to beat the default (_be thorough_ when the agent is already thorough-ish) is a no-op, and the fix is a stronger word (_relentless_), not a different technique.


ARGUMENTS: use this skill to author the documents. Dont commit


---

## Assistant  ·  2026-08-26T15:18:27.385Z

Authorized. Reading the two formats I must match before writing anything.


---

## Assistant  ·  2026-08-26T15:18:28.098Z

`[tool] Bash — Read CONTEXT.md format spec`


---

## Assistant  ·  2026-08-26T15:18:28.906Z

`[tool] Bash — Read prettier config to match formatting`


---

## Assistant  ·  2026-08-26T15:18:34.235Z

`[tool] Bash — Read CONTEXT.md format spec with absolute path`


---

## Assistant  ·  2026-08-26T15:18:34.794Z

`[tool] Bash — Read prettier config with absolute path`


---

## Assistant  ·  2026-08-26T15:19:35.378Z

Formats confirmed — `proseWrap` defaults to preserve, so hand-wrapping survives; `printWidth: 120` governs tables. Writing now, starting with the two vision documents.


---

## Assistant  ·  2026-08-26T15:20:14.106Z

`[tool] Write`


---

## Assistant  ·  2026-08-26T15:21:02.444Z

`[tool] Write`


---

## Assistant  ·  2026-08-26T15:22:04.254Z

Now the glossary. Adding only terms the vision documents actually use, and keeping the behavioural rule out of it — that belongs in `AGENTS.md`, where it's always loaded.


---

## Assistant  ·  2026-08-26T15:22:06.400Z

`[tool] Edit`


---

## Assistant  ·  2026-08-26T15:22:16.077Z

`[tool] Edit`


---

## Assistant  ·  2026-08-26T15:22:20.294Z

`[tool] Edit`


---

## Assistant  ·  2026-08-26T15:22:32.019Z

`[tool] Edit`


---

## Assistant  ·  2026-08-26T15:22:55.171Z

`AGENTS.md` is always loaded, so it gets the behavioural rule and two pointers — nothing the vision documents already carry.


---

## Assistant  ·  2026-08-26T15:22:57.978Z

`[tool] Edit`


---

## Assistant  ·  2026-08-26T15:23:03.878Z

`[tool] Edit`


---

## Assistant  ·  2026-08-26T15:23:34.371Z

Now the front door. Neither repo has ever had one, and there's no working CLI yet — so status goes early and honestly rather than a quickstart for something that doesn't exist.


---

## Assistant  ·  2026-08-26T15:24:07.235Z

`[tool] Write`


---

## Assistant  ·  2026-08-26T15:24:12.786Z

Formatting to the repo's gate, then running the full verification the pre-push hook runs.


---

## Assistant  ·  2026-08-26T15:24:13.920Z

`[tool] Bash — Format new and changed markdown files`


---

## Assistant  ·  2026-08-26T15:24:19.561Z

`[tool] Bash — Run the full verification gate`


---

## Assistant  ·  2026-08-26T15:24:43.701Z

All green. One self-check before I report — I introduced a rule that bare "harness" is ambiguous, so my own documents had better comply:


---

## Assistant  ·  2026-08-26T15:24:45.081Z

`[tool] Bash — Check my own documents obey the naming rule`


---

## Assistant  ·  2026-08-26T15:24:53.645Z

`[tool] Bash — List every bare-harness match per file`


---

## Assistant  ·  2026-08-26T15:25:35.796Z

Written, formatted, and the full gate is green. **Nothing committed.**

| file | change |
|---|---|
| `docs/vision/product.md` | **new** — promise, the four Guarantee tiers, two roles, LLM-wiki, boundaries, horizons |
| `docs/vision/architecture.md` | **new** — 11 tenets with reasoning, the four cheap-now decisions, what's deliberately open |
| `README.md` | **new** — the first README either repo has ever had |
| `AGENTS.md` | + `## Naming` (the harness rule) and + `## Vision` (two pointers) |
| `CONTEXT.md` | opener de-asserts MCP; **Harness** → `markdown-harness` + **Host harness**; + Operator, Contributor, LLM-wiki, and a `What is promised` section with Guarantee, Signal, Authoring/Consumption path, Loosening |

`npm run verify` → `archgate check` `{"pass":true,"total":12,"unparsedAdrs":[]}` (both discriminators #32 specifies), eslint clean, prettier clean, `tsc` clean, 70 tests, knip clean.

**Two `archgate` warnings, both pre-existing and neither caused by this change** — reporting because warnings shouldn't be swallowed: `GEN-001`'s `Decision` section is 7,938 chars against a 2,000 cap and `Do's and Don'ts` is 3,897. That's the `review-context --verbose` truncation `CONTEXT.md`'s **Briefing** entry already documents, so it's cosmetic to the reporting surface, not to the ADR. (Also unrelated: `vitest.config.ts` emits a Vite `configLoader` deprecation notice.)

## Writing decisions worth knowing about

**The behavioural rule went in `AGENTS.md`, the definitions in `CONTEXT.md`** — not both. `AGENTS.md` is always loaded, so "ask which harness is meant" has to live there; duplicating it into the glossary would have made one meaning editable in two places. The glossary carries only `_Avoid_: the harness` on each entry. My own five documents were then checked against the rule — the only bare uses left are the two `_Avoid_` lines that exist to name the forbidden form.

**Both vision documents open with a derivation test, not with prose.** That's the whole reason they exist per your framing: four numbered questions a future agent runs before proposing anything, with the reference material below as what the test consults. The product test gates on Guarantee tier / which role / boundaries / horizon; the architecture test makes tenets 2 and 3 hard stops rather than trade-offs.

**Boundaries are written positive-first.** Steering by prohibition makes the forbidden thing more available, not less, so every "does not" row states what the product does *instead* — "delegates judgment to the host harness, on the user's own auth and bill" rather than "never calls a model."

**No frontmatter on the README**, though its sibling in `typescript-ai-harness` has some. Adding frontmatter before a config governs the file creates something that looks governed and isn't — which is exactly the confusion tenet 6 exists to avoid. Worth revisiting the day a real config exists; it's good dogfooding then.

## Three things I did not do

**The provisional-decisions issue is not created.** You said don't commit, and posting to GitHub is a remote write. Say the word and I'll open it with the twelve provisional items, the withdrawn read-counting decision and its reasoning, linked from `AGENTS.md`.

**No `@reviewer` or `@lessons-learned`.** These are documentation changes, which the workflow framework excludes — and more to the point, `lessons-learned` proposes ADRs, which would directly contradict your instruction not to finalise decisions yet. One thing it would legitimately have caught: **tenet 7 names an archgate ADR that should exist** for the dependency-admission procedure. That's a real follow-up, for `archgate:adr-author`, when you want it.

**No `LICENSE`.** Q18 said permissive, so the README says the intent — and also says that until the file exists, no licence is granted. That's legally accurate and slightly forbidding for a repo you may want to show around. A two-minute fix whenever you pick MIT or Apache-2.0.

## One caveat on the README you should spot-check before it's public

The "Why not an existing tool" table makes competitive claims about six named products. **Verified line-by-line in the tree:** the first-match-wins citations, Jekyll's `new_path.length` and `path.start_with?` defects, the `"default": false` quote, plus everything I measured myself (dependency footprints, `yaml`'s comment round-trip and its flow-collection reformatting, Node's segment-aware `matchesGlob`, and the OKF sections). **Second-hand from the research survey, not yet line-verified:** the `remark-lint-frontmatter-schema` last-match-wins and AJV-description claims, markdownlint's opaque string list, Obsidian Linter's `string → string`, Astro's silent key stripping, and JSON Schema Core §10.2. All are in `docs/research/` with citations — worth a pass before that table faces an audience that might include someone who maintains one of them.


---

## Human  ·  2026-08-26T15:30:50.836Z

create a ticket to capture the features and details we've discussed.


---

## Assistant  ·  2026-08-26T15:31:01.671Z

`[tool] Bash — Read issue tracker conventions`


---

## Assistant  ·  2026-08-26T15:31:03.170Z

`[tool] Bash — List repo labels and existing issues`


---

## Assistant  ·  2026-08-26T15:31:54.223Z

This will be the repo's first issue. Writing the body to a file first so the backticks and tables survive intact.


---

## Assistant  ·  2026-08-26T15:32:50.619Z

`[tool] Write`


---

## Assistant  ·  2026-08-26T15:32:59.183Z

`[tool] Bash — Create the capture ticket`


---

## Assistant  ·  2026-08-26T15:33:10.954Z

Created as **issue #1**. Wiring it into `AGENTS.md` so future sessions find it:


---

## Assistant  ·  2026-08-26T15:33:13.775Z

`[tool] Edit`


---

## Assistant  ·  2026-08-26T15:33:19.285Z

`[tool] Bash — Format and re-verify after the AGENTS.md edit`


---

## Assistant  ·  2026-08-26T15:33:41.712Z

Ticket created: **https://github.com/hancrafted/markdown-harness/issues/1** — the repo's first issue. Gate still green, and `AGENTS.md` now points at it.

## What's in it

Eight sections, organised so an item can be found by the question that sends you looking:

| § | holds |
|---|---|
| **A** | Signal and freshness — the per-path horizon, `stale_after` as derivable, declared-vs-observed staleness, signal ≠ action |
| **B** | Writes — derived-field only, `--check` default, and the ⚠️ tension with the twice-stated "report, never rewrite" instruction |
| **C** | Index generation — **open**, because OKF already ships `regenerate_indexes()` |
| **D** | Steering channel — CLI + generated doc first, MCP deferred, plus the ⚠️ contradiction with `CONTEXT.md` and #32 and how it resolves |
| **E–F** | Module 2 as reviewability; the unconfirmed config filename and the `frontmatter:` collision |
| **G** | Dependencies — the admission procedure and **every number measured this session** |
| **H** | Roles, git, and the undesigned `mh init`-on-Windows risk |

Plus three sections that exist to stop work being repeated: **Contested** (severity tiers — not merely open, and it decides whether "37 documents are stale" can be a Rule at all), **Withdrawn with reasons** (read-counting, with the five reasons and the condition under which it could return), and **Still open** (report format, suppression, the skills, the AFK report, CI/npm, `LICENSE`).

## Three judgment calls in how I wrote it

**No label.** Only `wontfix` of the five canonical triage labels exists in this repo — `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human` were never created. `documentation` was the closest existing fit and it's wrong: this isn't a request to change docs. Say the word and I'll create the canonical five and label it, or add `documentation` if you'd rather not grow the label set.

**It references the vision docs rather than restating them.** The tenets and the four cheap-now decisions live in `docs/vision/architecture.md` and are pointed at, not copied — one meaning, one place to edit.

**The measured numbers are in the ticket, not the vision docs.** Dependency footprints, `path.matchesGlob`'s segment-awareness, `yaml`'s comment-vs-byte round-trip behaviour. Vision documents shouldn't carry facts with an expiry date; a ticket is the right shelf, and the pointer in `AGENTS.md` triggers on "before measuring something a second time."

One flag carried forward from the last message, still true: the README's competitor table has six claims that came from the research survey rather than my own line-by-line verification. Worth a pass before that file has an audience.


---

## Human  ·  2026-08-26T15:37:10.320Z

finally, this session was a great example of a grill-me, which I want to keep, to generate some workshop material. in docs/workshop/grill/vision/ 
create a raw.md which saves the entire conversion verbatim.


---

## Assistant  ·  2026-08-26T15:37:29.881Z

Before I write anything: a reconstruction from my own context would be a paraphrase wearing the word "verbatim" — exactly the failure mode this session is about. Let me find the actual session transcript on disk instead.


---

## Assistant  ·  2026-08-26T15:37:31.515Z

`[tool] Bash — Look for session transcript files`


---

## Assistant  ·  2026-08-26T15:37:39.050Z

`2a14b75a…` matches this session's ID. Inspecting its structure without pulling 108K into context:


---

## Assistant  ·  2026-08-26T15:37:42.414Z

`[tool] Bash — Inspect transcript JSONL schema`


---

## Assistant  ·  2026-08-26T15:37:55.982Z

Only 3 assistant records in 44 — that's not this conversation. Let me find which transcript actually holds it:


---

## Assistant  ·  2026-08-26T15:37:59.372Z

`[tool] Bash — Identify which transcript holds this conversation`


---

## Assistant  ·  2026-08-26T15:38:12.728Z

`7e0752df` — 111 assistant records, 65 user, and "host harness" 18 times. Verifying it's this session before I trust it:


---

## Assistant  ·  2026-08-26T15:38:16.954Z

`[tool] Bash — Confirm transcript identity and structure`


---

## Assistant  ·  2026-08-26T15:38:43.853Z

Confirmed — 13:21:38Z to 15:37:59Z today, all main-thread, and the tail matches exactly. One hazard to handle before writing: **prettier would silently reformat a verbatim transcript**, which is precisely why `docs/okf/SPEC-v0.2.md` sits in `.prettierignore`. Measuring the payload first:


---

## Assistant  ·  2026-08-26T15:38:47.731Z

`[tool] Bash — Measure dialogue vs full transcript size`
