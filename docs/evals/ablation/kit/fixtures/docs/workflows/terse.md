---
type: workflow
title: ci
description: A workflow whose title is too short to identify it.
---

FAILS `minLength: 3` on `title`. Two characters, and the constraint names strings
— which is why a two-ENTRY list would not satisfy it either, and why that case is
a shape collision rather than a short value.
